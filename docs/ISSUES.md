# Issue register — pipeline (2026-08-09)

Stable codes for the
`feature_selection → final_features → train_test_creator → model → result_evaluator`
chain. **Codes are permanent**: a resolved issue keeps its code and its row, never
renumbered and never reused, so an old message that says "BNK-1" still resolves.

> ### The four root registers — one job each, no overlap
>
> | file | answers | when you touch it |
> |---|---|---|
> | **[CLAUDE.md](../CLAUDE.md)** | *what is this, and what has it PROVED?* | auto-loaded every session; the map and the verdict |
> | **[RUNBOOK.md](RUNBOOK.md)** | *how do I RUN it?* | commands, stage order, the flags that destroy things |
> | **[ISSUES.md](ISSUES.md)** | *what is BROKEN?* | permanent codes; a code is never renumbered or reused |
> | **[TODO.md](TODO.md)** | *what is NEXT?* | one list, `P1` first. ⚠️ **A bare `P<n>` is LIVE; a HYPHENATED code (`P1-9`, `PRF-8`, `M-3`) is RETIRED** — renumbered 2026-08-21, crosswalk at the top of that file |
>
> Movement between them is one-way and worth knowing: a TODO item that turns out to be a
> defect **graduates to ISSUES.md with a code**; an ISSUES entry that gets fixed keeps its
> row and is struck through; a TODO item that gets done leaves its measurement in CLAUDE.md
> or a `CONTEXT.md` and is **deleted, not ticked**. Anything a future session must not
> rediscover belongs in CLAUDE.md, not here.


## Open (28)

| code | severity | issue | lives in |
|---|---|---|---|
| **ORT-2** | substantive — ⚠️ **AN onnxruntime VERSION *INSIDE* THE SUPPORTED LINE CHANGES WHAT THE OCR READS** | ⚠️ **ALIGNING THE VERSION NUMBERS MADE THE OUTPUTS DIVERGE.** Measured 2026-08-28 over four Kaggle T4 runs of one filing (VCB Q1-2026, whose 98 cells are on disk), with opencv varied to exonerate it: **`onnxruntime-gpu 1.22.0` → `onnx@200`, REPRODUCED 98/98**; `1.20.2` → **`onnx@200` FAILS**, the cascade escalates to `onnx@300`, and THAT layer writes a **ROW-SLID income statement** — 4 cells each carrying their neighbour's figure (`x_chi_phi_du_phong_rui_ro_tin_dung` holds `ix_loi_nhuan_thuan…`'s 14,295,755 mn) — which **`reconcile` AND `sane` both accept**. `SLD-1`'s shape, reached by a library downgrade. ⚠️ **opencv is NOT the cause**: the third run left BOTH of Kaggle's opencv distributions untouched at 4.13.0.92 and still failed. ⚠️ **Both versions are inside the CUDA 12 / cuDNN 9 line `ORT-1` prescribes**, so "supported" does not mean "equivalent". ✅ **RESOLVED BY ALIGNING UPWARD INSTEAD — `onnxruntime-gpu==1.22.0` ON BOTH.** The range-pin was a first answer drawn from ONE direction: only the DOWNWARD move (Kaggle to this machine's 1.20.2) had been tried. Moving THIS machine UP to 1.22.0 reproduces 98 of 98 here as well, so a single version holds — verified by an interleaved pair 2026-08-28. **The rule this earns: "same version" is not the goal, "same OUTPUT" is; pin only what is verified to reproduce on BOTH machines.** CLAUDE.md §6-2-duodetricies | `src/web_scraper/requirements-ocr.txt`, `src/web_scraper/pdf_ocr_job.py` |
| **CRP-1** | substantive — ✅ **(1) THE WRONG CASH FIGURE IS FIXED 2026-08-28; (2) THE INCOME STATEMENT IS STILL ABSENT, AND ITS RECORDED CAUSE WAS WRONG** | ✅ **(1) FIXED, and the mechanism turned out to be a MERGED LABEL rather than a missing anchor.** The filing prints the FX line with **no figure**, so `table_rows` carried its label forward and the closing balance's figures arrived under both labels joined — and `slug` caps a key at 60 characters, which is exactly where "…tương đương tiền cuối kỳ" was cut off. The row therefore read as a pure FX line. `_split_fx_from_balance` moves the figure to the closing column and **leaves FX empty** (the filing printed none; §5 rule 2). ⚠️ **IT IS IN THE DEFAULT PATH, NOT A LAYER FLAG, AND THAT IS THE REUSABLE PART**: VIC's cash flow is accepted at `onnx@200`, **layer 1 of 47**, so a late-cascade layer could never be reached — `PGB-1` and §6-2-unvicies each recorded the same trap from the other side. Verified on the real document: closing **54,750,360 mn**, FX empty, and `_cash_flow_identity` now CLOSES to the đồng (72,226,561 − 17,476,201 + 0). ⚠️ **`_probe` also stopped answering with the opening balance**, which matters beyond this quarter — it is the figure `sane` bands a cash flow on, so a wrong one poisons the magnitude history of every quarter after it (`SAN-1`). ⚠️ **(2) STILL OPEN, AND THE CAUSE RECORDED HERE ON 2026-08-28 WAS WRONG.** This entry said *"it is a `_page_kind` classification failure and not an OCR one"*. Measured directly by dumping the page scan: pages 9 and 10 — the income statement, between the balance sheet on 6-9 and the cash flow on 12-13 — come back with **25 and 5 words** against **94-169** on every neighbouring page, and headers reading `I s / E / 3 / co`. **The OCR read almost nothing there, so the classifier had nothing to classify.** The pages are structurally identical to their neighbours (612×792, rotation 0, four DeviceGray strips each), so it is not geometry; no cause beyond that is established and none is guessed at here. The remaining defects, verbatim from 2026-08-28: **(1) THE CLOSING CASH BALANCE IS WRITTEN INTO THE FX-ADJUSTMENT COLUMN.** The corp chart prints `(60)` opening, `(61)` FX effect, `(70) = 50+60+61` closing; the run put **54,750,360 mn into `hdtc_anh_huong_cua_thay_doi_ty_gia_hoi_doai_quy_doi_ngoai_te`** and left `…_cuoi_ky_70_50_60_61` **empty**. Proven three independent ways, no PDF needed: opening **72,226,561** + net **−17,476,201** = **54,750,360** exactly; the SAME filing's balance sheet carries `i_tien_va_cac_khoan_tuong_duong_tien` = **54,750,360**; and its two components (47,992,026 + 6,758,334) sum to it to the đồng. A 54.75 tn FX effect on 72 tn of cash is not a number a company reports. ⚠️ **It was ACCEPTED at `onnx@200`** — a STRICT layer, where `verify_cash` rides with `relax_totals` and is therefore OFF, so the identity never ran; and `sane` failed open because VIC has no accepted quarter on disk to build a band from. Same shape as `FXM-1`/`P39` from the other direction: there merger cash went INTO the FX column, here the closing balance does. **(2) THE INCOME STATEMENT IS NEVER FOUND** — `no such statement on any page of this filing`, at **all 47 layers**, so it is a `_page_kind` classification failure and not an OCR one: the balance sheet was found on pages 6-9 and the cash flow on 12-13, so the P&L sits between them unclassified. ⚠️ **The corp template READS well and that is the trap** — 79 balance-sheet items mapped, more than any bank statement here, and `reconcile` passed on the TRIVIAL identity (assets == resources, both 1,178,694,748 mn) because `C_LIABILITIES` maps to nothing on corp, so nothing ever checked assets == liabilities + equity. **Nothing from this run may be quoted as a fundamental.** Blocks `P5`/`P6` for the 761 non-bank names beside `TPL-1`. CLAUDE.md §6-2-septvicies. ⚠️ **(3) FIELD EVIDENCE, 2026-08-29 — 27 QUARTERS, AND THE BALANCE SHEET SPLITS ON ASSURANCE.** The first authoritative VIC run (`skip_existing: false`, no `periods`) was stopped by hand at **27 of 72 quarters** after 12 h, and its statement CSVs are the first non-bank corpus this repo has: **balance sheet 13 `pdf` / 14 `missing`, income statement 21 / 6, cash flow 20 / 7 — 0 rows from any HTML tab**. ⚠️ **The balance sheet fails on SELF-PREPARED quarterlies and nowhere else: 12/13 on audited or reviewed filings against 1/14 on unaudited ones**, while the income statement (10/13 vs 11/14) and the cash flow (7/9 vs 13/18) show no such split — so it is not scan quality, which would take all three down together. ⚠️ **The mechanism is indicated as the COMPARATIVE COLUMN and it is `sane` that sees it**: the gate refused Q1-2009, Q3-2009 and Q4-2009 on the *same* probe (6.02e+12) and Q1-2010 and Q3-2010 on another (1.43e+13) — a balance sheet printing its prior year-end beside the current period, with the parser taking the prior one, which is a quarter the run had already accepted. `reconcile` passes on such a column (it is internally consistent), so `sane`'s equality gate is the only thing standing between this and 14 wrong balance sheets written as `pdf`. ⚠️ **NOT VERIFIED against the PDF's own two columns** — the assurance split, the repeated probes and the refusal mix (11 × `assets != liabilities + equity`, 3 × `no total assets`) are what is measured. ⚠️ **This re-shapes `P5` rather than sizing it**: §6-2-untricies fixed the anchors on ONE quarter, and 27 quarters say the anchors were never this statement's problem. CLAUDE.md §6-2-duotricies | `src/web_scraper/cafef_financials.py` (`_align`/`_anchor`, `C_LIABILITIES`), `src/web_scraper/cafef_pdf_parser.py` (`_page_kind`) |
| **ORT-1** | substantive — ⚠️ **A GREEN GPU RUN CAN BE HALF ON THE CPU AND SAY NOTHING** | ⚠️ **`onnxruntime.get_available_providers()` IS AN ADVERTISEMENT, NOT A MEASUREMENT — and the session downgrades SILENTLY.** Measured on the first Kaggle T4 parse, 2026-08-28: a bare `pip install onnxruntime-gpu` resolved to **1.29.0**, which requires **cuDNN 9 with CUDA 13**, against Kaggle's **CUDA 12.8**. `get_available_providers()` still listed `CUDAExecutionProvider`; `InferenceSession` then failed to create it and fell back to `CPUExecutionProvider`, so **DeepDoc DB detection ran on the worker's CPU while VietOCR ran on the T4** — a run that was CORRECT (98 of 98 cells reproduced) and **21 % slower** (83.8 s against 69.0 s once the provider loaded). ⚠️ **The only trace was one warning inside a wall of ANSI-coloured onnxruntime noise**, and `_DbTextDetector` chose its provider list from the advertisement, so nothing downstream could tell the two runs apart. ✅ **FIXED 2026-08-28**, three ways, none of them a version bump alone: `onnx_ocr` calls `ort.preload_dlls()` where it exists (1.21+ no longer adds the `nvidia-*` wheels to the loader path itself, which is the underlying cause on a pip-only CUDA image); `pdf_ocr_job.engine_report()` reads back **`session.get_providers()`** and records it in every run's `metadata.json`, so an artefact says which half of the OCR ran where; and `RUN__pdf_ocr` pins the **line** `onnxruntime-gpu>=1.19,<1.23`. ⚠️ **The `==1.20.1` pin was tried first and cost a run**: this repo's own version is not published for Kaggle's cp312 Linux (the index offers 1.20.0 and 1.20.2), so an exact pin fails the INSTALL. ⚠️ **The two OCR halves fail INDEPENDENTLY** — detection is onnxruntime, recognition is torch — so *"the GPU is being used"* is two questions, and `runtime.gpu_report()` answers neither of them. ⚠️ **The two pre-fix run folders carry `ocr: null`** — an absent measurement, never inferred (§5 rule 2). CLAUDE.md §6-2-sexvicies | `src/web_scraper/onnx_ocr.py`, `src/web_scraper/pdf_ocr_job.py`, `src/web_scraper/RUN__pdf_ocr.ipynb` |
| **CFB-1** | substantive — ⚠️ **WRONG NUMBERS ON DISK TODAY; READ THIS BEFORE QUOTING A BID CASH FLOW** | ⚠️ **A CASH-FLOW ANCHOR CAN HOLD THE WRONG ACCOUNT AND EVERY GATE PASSES.** These filings print a cash flow that is CUMULATIVE from 1 January, so every quarter of a year shares one opening balance and it equals the prior year's Q4 closing — five independent readings of one number. Nothing in the code checks it. Measured 2026-08-27 over the three parsed tickers, **0 s and no OCR**: **7 BID quarters carry the 1-Jan opening in the CLOSING slot** (Q1-2013, Q3-2013, Q1-2014, Q1-2016, Q1-2017, Q3-2019, Q3-2020), **2 carry it in the MOVEMENT slot** (Q1-2020, Q1-2021), **4 hold a wrong FX value** (Q4-2011 48,919,272 and Q2-2012 40,110,402 are cash BALANCES; Q4-2009 and Q4-2012 hold a literal 0), and **BID Q3-2011 holds a closing cash balance of −23,457,326,032,339**. ⚠️ **Every one of the 7 was accepted at `onnx@200`** — a STRICT layer, where `verify_cash` rides with `relax_totals` and is therefore OFF, so `_cash_flow_identity` never ran and nothing tied the closing balance back to the opening one. `reconcile` performs no sign test, `sane` compares `abs()`, and `_closing_breakdown` fails open when the filing prints no component list — so a negative cash balance passes all three. ⚠️ **ACB is clean on every signature; VCB's 17 opening mismatches are MIXED** — Q1-Q3 2023 differ from Q4-2022 by exactly one digit (412,235,294 vs 412,135,294, an OCR error) while 2018 and 2024 may be genuine restatements — so the check is a SCREEN producing candidates, never a verdict. ⚠️ **This is NOT `FXM-1`**: that code is the positional FX guess, whose two cells were fixed by `P39`; these rows come from `_align` / `_anchor` at strict layers and no `P39` change reaches them. **TODO `P43` is the fix** and is the top row of the list. CLAUDE.md §6-2-quinvicies |
| **PGB-1** | substantive — ⚠️ **FIXED 2026-08-26 behind a parse layer; the DEFAULT still over-includes** | ⚠️ **WHEN NO FORM CODE SURVIVES OCR, NOTHING PRUNES THE NOTES OUT OF A STATEMENT.** `_drop_islands` measures a stray title page's distance from a FORM-CODED one and returns early with no anchor to measure from (`if not anchors: continue`). BID's Q3-2025 filing reads `from_form = False` on **all 37 pages**, so pages 12-13 and 18-34 — notes whose headers score against the balance-sheet title — were kept, and `_fill_continuations` then swept every numbered table after them in. The "balance sheet" came out as **22 pages / 316 rows** and its grand-total anchors were taken from a NOTE table: `tong_tai_san` = **115,110 mn** for a bank whose real total is **3,071,970,196 mn**. ⚠️ **`reconcile` PASSES on that**, because assets and resources are the same piece of garbage; **`sane` is the only gate that refuses it**, which is why the quarter read `missing` rather than wrong. ⚠️ **A SECOND DEFECT RIDES WITH IT**: the same filing merges "B. NỢ PHẢI TRẢ VÀ VỐN CHỦ SỞ HỮU" with "I. Các khoản nợ Chính phủ và NHNN" into one row, and `_anchor` scored `row.key` RAW where the ordered walk in `map_to_schema` has always scored `_split_merged(...)` — so "vốn chủ sở hữu" sat inside the merged label and the EQUITY anchor answered with the government-debt figure, 215,823,611 (**7.0 % of assets** against BID's usual 5.2-5.4 %). ⚠️ **The same symptom is on disk in three quarters nobody has checked** — BID Q1-2024 E/A **2.4 %**, Q3-2024 **3.7 %**, Q1-2026 **7.4 %**. **Fixed** by `notes_boundary` (a title-only page may not re-open a report once the notes have begun, provided that report already had a run BEFORE them — the second condition is what stops a CONTENTS page pruning the whole filing) and `relax_merged_seam` (`MERGED_SEAM_RE_LOOSE` admits a single-letter roman numeral; `_anchor` scores the split key). Both are ParseLayer flags, off by default, last in the cascade. ⚠️ **ORDER IS LOAD-BEARING AND WAS WRONG FIRST TIME**: `+notes` alone ACCEPTS the statement — both totals are then correct, which is all `reconcile` and `sane` inspect — while equity stays wrong, so the narrower layer must run AFTER the seam-relaxed one or it writes a wrong number and ends the cascade. BID Q3-2025 now reads `pdf` at `onnx@200+notes+seam`, 3 pages / 73 rows, total assets 3,071,970,196 mn, equity ABSENT. | `web_scraper/cafef_pdf_parser.py` `_drop_after_notes`, `_drop_islands`, `_fill_continuations`; `cafef_financials.py` `_anchor`, `MERGED_SEAM_RE_LOOSE` |
| **SLD-1** | ⚠️ **CORRECTNESS — WRONG FIGURES ARE ON DISK, 5 CONFIRMED** | ⚠️ **A STATEMENT CAN BE PARSED ONE ROW OUT OF ALIGNMENT, PASS EVERY GATE, AND BE WRITTEN AS `pdf` WITH EVERY LINE ITEM WRONG.** On some scans the OCR emits each numeric box a constant distance ABOVE the text box of its own printed line — BID Q1-2021 by 7pt, past `Y_TOL = 4.0`, so the two never group. `table_rows` emits a row only when it holds a figure and turns a label-only line into `carry`, so a SECTION HEADING (`A. TÀI SẢN`) leaves a label waiting and every figure is then handed to the label ABOVE it. The existing recovery for this (the ACB Q1-2024 branch, "a label sits BELOW its own figures") is disabled exactly then, because its condition is `not words_` and `carry` is not empty. ⚠️ **BOTH GRAND TOTALS CAN STILL LAND CORRECTLY**, so `reconcile` passes and nothing raises. ⚠️ **VERIFIED AGAINST THE FILING'S OWN TEXT**: VCB Q1-2021 page 4 prints `Tiền mặt, vàng bạc, đá quý` 12,277,634 and `Tiền gửi tại NHNN` 24,008,360; the row on disk holds 24,008,360 and 203,604,117 — each label carrying the NEXT line's figure. **Audit 2026-08-26** re-OCR'd all 201 ACB+VCB+BID filings (69.9 min): **20 of 596 statement-rows carry a non-zero offset, 18 of them stored as `pdf`, and NOT ONE is benign** — re-pairing changes 2 to 22 line items in every case. Judging the 6 balance sheets against the prior Q4 already accepted on disk (their comparative column must reproduce it) convicts **5**: BID Q2-2019 (15/45 → 46/46), BID Q1-2022 (26/44 → 45/46), VCB Q1-2021 (31/47 → 45/46), BID Q2-2014 (24/36 → 47/52), VCB Q1-2026 (42/48 → 56/56); VCB Q3-2023 (47/51 → 52/61) is inconclusive and probably sound. ⚠️ **5 IS A LOWER BOUND, NOT A TOTAL**: the other 12 are income statements and cash flows whose comparative column is the prior YEAR's period, which is not a stored quarter, so this method cannot judge them — and **3 of those sit in a filing whose balance sheet is already convicted** (BID Q1-2021 changes 9 items, BID Q2-2019 13, VCB Q1-2026 5). Worst unjudged: BID Q4-2024 income statement, offset 11.5, 22 items. ⚠️ **OFFSET SIZE DOES NOT PREDICT DAMAGE** — BID Q2-2019's income statement has the smallest offset measured (2.0) and changes 13 items. ⚠️ **THE FIX SHIPPED 2026-08-26 DOES NOT REPAIR ANY OF THE 5.** `realign_rows` is a `ParseLayer` flag and its five layers sit LAST, so only a statement that defeated all 26 earlier layers reaches them — every convicted row reconciles at layer 1 and stops the cascade. It recovered BID Q1-2021 (`missing` → `pdf`, total assets 1,558,887,407 mn, the figure printed on page 2) and can reach exactly **2 of the 25 `missing` statement-rows**; the other 23 measure offset 0. Repairing the 5 needs a decision to re-judge accepted statements, which is a separate change. | `web_scraper/cafef_pdf_parser.py` `table_rows`, `_value_row_offset`, `Y_TOL`; the audit is `CLAUDE.md` §6-2-sexdecies |
| **FXM-1** | substantive — ⚠️ **THE FIX IS MEASURED WRONG *AND* THE PROBLEM IS MOSTLY NOT REAL, 2026-08-27** | ⚠️ **THE FX ADJUSTMENT LINE CANNOT BE MAPPED, AND IT IS THE SINGLE BOTTLENECK BEHIND MOST CASH-FLOW REFUSALS.** Measured 2026-08-25 by putting each of BID's 13 genuinely failed statements through the full 26-layer cascade (3 h 56 m, all 13 completed) and reading the refusal reasons the log now keeps: **8 of the 13 end at `cash flow unverifiable — fx not mapped`** (6 on that alone, 2 with `opening` beside it). ⚠️ **The other 5 are a different problem and no FX change touches them**: 3 balance sheets (`no total assets`, `no total to balance against`), one cash flow out by five orders of magnitude (Q1-2026) and Q4-2010's identity that ran and did not close. The relaxed layers DO recover the opening and closing balances positionally (`_recover_totals`, `CASH_TAIL`) and then cannot PROVE them, because `_cash_flow_identity` needs all four terms and `VI. Điều chỉnh ảnh hưởng của thay đổi tỷ giá` does not reach `SCHEMA_MATCH = 0.80` on these scans. ⚠️ **So these quarters are not lost to unreadable pixels — the figures are recovered and then discarded for want of a fourth term.** The `fx = 0` stand-in already in `_cash_flow_identity` cannot help: it applies only when `open_ + net == close` to the đồng, which is exactly what is not true here. ⚠️ **The fix is written and its effect is UNMEASURED**: `_recover_totals` now claims the row between the recovered opening and closing when they sit EXACTLY two apart, which is the chart's own order (`HDTC_48` opening, `HDTC_49` FX, `HDTC_50` closing) and the same positional guess the IV recovery beside it already makes. It loosens no gate — it lets a gate that was skipping for want of a term actually run, and `_cash_flow_identity` then rejects a wrong row rather than writing it. ⚠️ **DO NOT ASSUME IT RECOVERS 8 QUARTERS**: BID's Q4-2010 reached the identity with all four terms and still failed (`cash flow does not close`), which is what a genuinely mis-read statement looks like and may be what several of these are. TODO `P39` is the measurement, and it must include a regression over ACB + VCB + BID before anything is believed. CLAUDE.md §6-2-quindecies. ⚠️ **UPDATE 2026-08-27 — THE FIX'S OWN SAFETY ARGUMENT IS FALSE, AND BID Q3-2015 IS THE COUNTEREXAMPLE.** That quarter was one of the 8 and it turns out to have **no FX line at all**: BID absorbed MHB in 2015, so its tail reads **V** opening, **VII** `Tiền và các khoản tương đương tiền từ việc nhận sáp nhập MHB`, **VIII** closing, and the identity closes to the đồng on the MERGER line (50,202,708 + 16,160,895 + 1,477,340 = 67,840,943, residual **0**). ⚠️ **The row between the two balances IS the genuine fourth term — it is simply not FX — so `_cash_flow_identity` closes BECAUSE the row is right about the arithmetic and wrong about the account, and it cannot reject what it confirms.** Measured at `onnx@200+relax`: **`hdtc_vi_dieu_chinh_anh_huong_cua_thay_doi_ty_gia` = 1,477,340,000,000**, merger cash in the FX column. Nothing reached disk only because that layer was refused for an unrelated reason (`opening not mapped`). ⚠️ **This is `SLD-1`'s shape a third time — a wrong figure that passes every gate — and it is STRUCTURAL, not a threshold.** Before `P39` may run, the recovery must either check that the claimed row's own label is consistent with FX, or write the term to a column that says *unidentified fourth term* rather than naming it FX. ⚠️ **The remaining population is 7, not 8** — Q3-2015 parsed on 2026-08-27 with no code change at all, via `PGB-1`'s `_anchor` seam split. CLAUDE.md §6-2-vicies. ⚠️ **AND THE POPULATION COLLAPSED FROM 8 TO 1 ON 2026-08-27, WITHOUT ANY FX CHANGE.** Re-running the seven remaining quarters recovered **six of them** — Q4-2015, Q3-2016, Q2-2017, Q3-2017, Q4-2017, Q3-2018 — and **four won at plain `onnx@200`**. What had blocked them was `PGB-1`'s `_anchor` seam split, shipped 2026-08-26, and nobody had re-run them since. ⚠️ **At a STRICT layer `verify_cash` is off and the cash identity is never demanded, so the FX line is not needed at all.** The 2026-08-25 probe recorded each statement's LAST refusal reason, and the last layer is always the most relaxed one — **a cascade's final refusal names the hardest path tried, not the blocking defect.** That single reading error is what made this issue look like the repo's main OCR bottleneck for two days. ⚠️ **The seventh, Q4-2016, parsed WRONG and was reverted** — closing 61,575,636 against the filing's printed 65,521,789, accepted by both gates with an identity residual of exactly +1e12. CLAUDE.md §6-2-duovicies. ✅ **AND Q4-2016 PARSED CORRECTLY ON 2026-08-27 — the population is 0.** Two blockers, neither of them FX: the movement figure had its LAST DIGIT outside the detector crop (`6.711.633` reads as `6.711.6.3` / `6.711.610` / `6.711.63)` at 200/300/400 dpi and correctly at `crop_pad=6`), and the filing prints a FOURTH term — merger cash, 3,004,011 — the chart of accounts has no column for. ⚠️ **THE FIX IS NOT THIS ISSUE'S WRITTEN ONE AND MUST NOT BE READ AS ITS VINDICATION.** The new `cash_extra_terms` flag does the opposite of the positional guess: it SUMS what the filing printed between the two balances, requires exact equality to the đồng, and writes the term NOWHERE — and it additionally REFUSES the positional FX claim when the row's own label does not say FX, which is the guard this row asks for. ⚠️ **The unsafe guess is guarded, not removed**: it is untouched on every layer that does not carry the new flag, and only three layers do, all at the very end of the cascade. CLAUDE.md §6-2-quatervicies. | `web_scraper/cafef_financials.py` `_recover_totals` (the CASH_FLOW branch), `_cash_flow_identity`, `C_CASH_FX`, `CASH_BALANCES`  ⚠️ **AND THE FIX'S GUARD WAS WIRED TO A FLAG, SO FORTY-FOUR OF THE FORTY-SEVEN LAYERS NEVER APPLIED IT — ✅ FIXED 2026-08-27 (`P39`).** `_recover_totals` refused the positional FX claim only under `cash_extra_terms`, i.e. on the three layers carrying that flag; `onnx@200+relax` is **layer 5** and had no guard at all. Read off `cf_HOSE_BID.csv`, the unguarded claim had **already written merger cash into the FX column twice**: **Q4-2015 `1,477,340`** (MHB, FY-2015 audited annual) and **Q2-2017 `1,540,994`** (LienVietPostBank), each confirmed by the identity to the đồng. The guard is unconditional now and `_recover_totals` takes no parameter that could switch it off — a knob that decides whether a guard applies is a knob that turns a guard off. ⚠️ **Blast radius MEASURED, not argued**: all 32 rows on disk that the branch could have produced (FX non-empty ∧ a `relax_totals` layer) were re-parsed at their own recorded layers — **30 unchanged, 2 dropped**, exactly the defect. Both quarters were re-parsed and keep `open`/`IV`/`close` unchanged with the fourth term now unattributed. ⚠️ **STILL OPEN under this code**: the alternate-filing half has no restatement guard and is measured to need `P43`'s forward check, and **four BID rows carry a wrong FX value written at STRICT layers** (Q4-2011 48,919,272 and Q2-2012 40,110,402 are cash balances) which this fix does not reach. CLAUDE.md §6-2-quinvicies  ⚠️ **THIS CODE STAYS OPEN AND NOW POINTS AT `P43`, NOT `P39` (2026-08-28).** `P39` closed with the FX half fixed and measured; what is left of this defect is the `alternates` restatement guard, and it **cannot be built as `P39` described** — BID's unaudited and audited Q4-2016 filings share the same opening balance, so the backward check is blind to their disagreement. The discriminator is the opening balance the **2017** quarters independently print, i.e. the forward half of `P43`'s cumulative cash-flow invariant. ⚠️ **A fixed HALF is not a fixed code**: the defect that put `61,575,636` on disk is still reachable through `alternates`. |
| **TPL-1** | substantive — ✅ **FIXED 2026-08-28, and the fix was measured on a real filing** | ✅ **THE ANCHORS ARE TEMPLATE-COMPLETE NOW.** The nine `C_*` role tuples cover all four charts of accounts and `ANCHORS` is **derived from them** rather than re-listed — the duplication was the defect, since adding a column to a role left the anchor list behind. `_anchor` re-matched **2 of 7 roles on `corp`** and now re-matches 7; `_cash_flow_identity` could not run outside `bank` at all and now runs. ⚠️ **The union is equivalent to a per-template table only because no chart answers a role twice**, which is asserted for all 4 templates × 3 reports rather than assumed (`test_cafef_financials_anchors.py`, 96 tests). ⚠️ **`Statement.find` gained a `reject` disqualifier** so the closing-cash needle can never be answered by a row saying `đầu kỳ` — a REFUSAL instead of a wrong figure. ⚠️ **BANK IS UNCHANGED BY CONSTRUCTION** (the added names exist in no bank chart) and by measurement: **15 of 15 statements across 5 filings of ACB, VCB and BID re-map identically**. ⚠️ **Two things remain and neither is an anchor**: `insurance` still has NO closing-cash line in its chart (a schema repair), and `C_LIABILITIES` still misses on `corp` in the field — the row is there (VIC 1,024,990,928 mn, and 1,024,990,928 + 153,703,820 = total assets exactly) but its label carries page-header text OCR merged onto it, and its account text `no_phai_tra` is **9 characters, below `MIN_CONTAINS = 10`**, so containment cannot reach it. That leaves a corp balance sheet reconciling on the TRIVIAL `assets == resources` identity. Loosening the containment floor would re-open the hazard `_anchor`'s new guard was just added for, so it is recorded rather than patched. **The original measurement, 2026-08-25:** ⚠️ **THE RECONCILE ANCHORS ARE BANK-SHAPED, SO THE THREE NON-BANK TEMPLATES DO NOT RECONCILE — AND TWO OF THEM FAIL SILENTLY.** All four charts of accounts EXIST (`raw_data/cafef/financials/schema/`, 12 files, 871 rows) and `detect_template` / `schema_of` / `map_to_schema` are template-generic, so the wall is **not** a missing template — it is `FinancialsBuilder`'s seven hardcoded canonical anchor tuples (`C_ASSETS` … `C_CASH_CLOSE`), which are EXACT dict-key lookups against bank column names. Measured 2026-08-25 by replaying `Statement.find`'s own algorithm over each chart's `as_printed` labels: **`C_PBT`, `C_NET_CF` and `C_CASH_CLOSE` miss on all three non-bank charts**, and `C_LIABILITIES` misses on all three (they print `c_no_phai_tra`, not `tong_no_phai_tra`). ⚠️ **The text fallback then does something WORSE THAN MISS**: `CASH_CLOSE`'s needle fuzzy-matches the **OPENING** balance at **0.885 (corp)** and **0.902 (insurance)** against `NAME_MATCH = 0.85`, and `find` scans in statement order where the opening line is printed FIRST — so `reconcile` and `_probe` are handed **`đầu kỳ` as `cuối kỳ`**, a wrong figure rather than a refusal. **`bank` is protected only because its canonical column is present and the fallback is never reached** (its own opening line scores 0.930). ⚠️ **`securities` is the opposite failure**: opening 0.789 / closing 0.831, both BELOW the threshold, so every securities cash flow is rejected for `no closing cash balance`. ⚠️ **And CafeF's insurance cash-flow chart has NO closing-balance line at all** — it ends at `HDTC_40` (FX effect) — so there is nowhere to STORE the figure even if OCR reads it off the PDF. ⚠️ `_cash_flow_identity`'s `C_CASH_OPEN` / `C_CASH_FX` / `C_FLOW_SECTIONS` are bank-only (0 of 3 present on any non-bank chart) and `TOTAL_ALIASES`' two recovery columns exist in no non-bank chart, so neither the identity check nor the `_anchor` recovery can run outside `bank`. ⚠️ **NOTHING ON DISK IS WRONG TODAY** — only ACB, VCB and BID have been parsed and all three are banks — so this is a wall in front of `P5`/`P38`, not a defect in an existing number. CLAUDE.md §6-2-quaterdecies; TODO `P5`. | `web_scraper/cafef_financials.py:217-225` (the `C_*` tuples), `:1402` `reconcile`, `:1655` `_probe`, `:1484-1488`, `:1117` `TOTAL_ALIASES`; `web_scraper/cafef_pdf_parser.py:79` `NAME_MATCH`, `:111` `find` |
| **GLB-1** | substantive ✅ **FIXED 2026-08-24** (one-line repair; the root cause is left) | ⚠️ **TWO STAR IMPORTS SILENTLY REBOUND `glob` FROM THE FUNCTION TO THE MODULE, BREAKING ALL 11 CALL SITES IN `preprocessor.py`.** Line 6 is `from glob import glob`; lines 35 and 37 are `from utils.enums import *` and `from utils.utils import *`, and **neither module declares `__all__`**, so `import *` carries their own `import glob` into this namespace and overwrites the function. Every `glob(...)` in the file then raises `TypeError: 'module' object is not callable` — the economy, forex, funds, indices, stocks, financials and statement ingests alike. Found 2026-08-24 when `bronze/cafef_financials` failed on it at `_ingest_bronze_cafef_financial_schema`. ⚠️ **NOT caused by that day's work** — `git log` puts `preprocessor.py` last touched at `82fea06`, and the file was not in the session's diff. ✅ **Fixed minimally**: `from glob import glob` re-bound AFTER the star imports, with a comment saying why it must stay there. ⚠️ **The root cause survives** — a future star import re-breaks it, and the real repair is an `__all__` in `utils.utils` and `utils.enums`, which is a wider change than this session should make. ⚠️ **The general shape: `import *` from a module without `__all__` exports that module's IMPORTS, not just its definitions.** | `orchestration/preprocessor/preprocessor.py` imports; every `glob(` call site |
| **BRZ-1** | substantive, **OPEN** | ⚠️ **A ROW DELETED AT THE SOURCE IS NEVER DELETED FROM BRONZE — the ingest UPSERTS.** Measured 2026-08-24 on the financials carry-up: after `period_min=Q1-2008` removed 8 VCB quarters from the statement CSVs and ACB lost Q2-2026, `bronze.cafef_financials_bank_*` still held **152 periods against the CSVs' 143** — VCB Q4-2006, Q1-2007..Q4-2007, Q1-2008..Q3-2008 and ACB Q2-2026 all survived as rows describing quarters that no longer exist upstream. `_helper_save_pandas_table_to_database` upserts on the PK, so it can ADD and UPDATE but never REMOVE. ⚠️ **This is CLAUDE.md §5 rule 11 in a sharper form**: *"re-scraped" never implies "re-ingested"* is about staleness, this is about **deletions that cannot propagate at all**, and no freshness check can see it — the row count is higher, not lower, and `MAX(date)` is correct. **Worked around** by `DROP TABLE` on the four bronze financials tables before re-ingesting (143 = 143 after, 0 stale). ⚠️ **Not fixed**: every bronze ingest in this repo has the same property, and a general fix is either a truncate-and-replace mode or a delete-what-is-absent pass — a decision, not a patch. | `preprocessor._helper_save_pandas_table_to_database`; every `_ingest_bronze_*` |
| **SAN-1** | substantive ✅ **FIXED 2026-08-24, the hour it was created** | ⚠️ **THE MAGNITUDE GUARD WAS POISONED BY THE DATA IT EXISTS TO CATCH, AND IT FAILED SILENTLY.** `sane` judges a statement against `history[report]` — the probes of quarters already accepted in the same run — using the **median** with a ±20× band. Turning on `allow_parent` (same day) let ACB's **Q3-2009 STANDALONE** filing into the run; its income statement mapped **2 line items**, reconciled, and its probe became the **only** entry in `history[income_statement]`. The median of one value IS that value, so the band became `that/20 … that×20` — and **ACB's Q1-2010 income statement, `pdf` at `onnx@200` for as long as the file has existed, was rejected as a magnitude outlier**. ⚠️ **Nothing raised**: the symptom is the word `absent` in a log line, indistinguishable from a document that genuinely cannot be read. Caught only by diffing the run against a pre-run backup. ⚠️ **The blast radius was every later quarter of that statement**, and the run writes NON-MERGING progress snapshots, so an interrupted run leaves a TRUNCATED csv — ACB's three files stood at 9 rows against 74 when the run was killed (restored byte-identical from backup; 0 rows lost). ✅ **FIXED two ways, both structural.** (1) **`history` is keyed by ENTITY as well as report** — a standalone company is not the consolidated group and its magnitudes are legitimately smaller, so pooling them makes the band meaningless in both directions; this became reachable the moment `allow_parent` existed, since a ticker can now file standalone early and consolidated later (exactly ACB: standalone 2008-09, consolidated 2010+). (2) **`MIN_ITEMS_FOR_HISTORY = 8`** — a thin statement is still WRITTEN but may not become a REFERENCE, with a WARNING naming the item count. *Accepting a statement and trusting it are different decisions.* **5 tests**, including one that reproduces the poisoning itself so the fix cannot be quietly undone. ⚠️ **The general lesson is `DEP-1`'s from the other side**: `DEP-1` was a monitor that blocked the repair path, this is a validator that adopts the corruption as its own baseline. **Ask of any guard that learns from its input: what happens when the first thing it learns is wrong?** CLAUDE.md §6-2-undecies. | `web_scraper/cafef_financials.py::sane/_probe/build` |
| **FIN-1** | substantive ✅ **FIXED 2026-08-24** — 0 rows carry `source='cafef'` at any layer; `bronze.cafef_financial_reports` reads **405 `pdf` / 24 `missing` / 0 other**, and the carry-up to `gold.stocks_financials_bank_fa` (8,669 rows, to 2026-08-21) is done. ✅ **The 2 cells that carried the metadata of a parse `_decumulate` threw away are corrected too** (ACB Q4-2009 IS, VCB Q4-2008 IS). `_write` now takes provenance only from a period that produced a row, and the same predicate was REPLAYED over the CSVs already on disk rather than paying a 5.5 h re-parse to change 2 of 429 cells — dry run 2 rows, applied, re-run 0 (idempotent), 2 lines in 2 files, `publish_date`/`assurance`/share counts deliberately untouched as per-DOCUMENT facts. Verified after re-ingest: **0 rows in `bronze.cafef_financial_reports` carry provenance without having produced a statement.** CLAUDE.md §6-2-duodecies. | ⚠️ **34 FINANCIAL REPORT-ROWS ON DISK WERE TRANSCRIBED FROM CafeF'S HTML TABS, NOT PARSED FROM THE FILING PDF — and only 4 of them can be retried from a document on disk.** Opened 2026-08-24, the day CLAUDE.md §5 rule 24 made the PDF the ONLY permitted source for a financial statement. `CafefFinancialsBuilder` takes **`use_api: bool = True`** (`cafef_financials.py:485`, `:1629`) and, after the PDF pass, fills every period still absent from CafeF's three web tabs — `meta[report][period] = {"source": "cafef"}` at `:1833`. **Measured from `bronze.cafef_financial_reports.source`: ACB 195 pdf / **27 cafef** / 0 missing; VCB 209 pdf / **7 cafef** / 18 missing.** ⚠️ **THE FALLBACK DOES NOT CHECK WHETHER A PDF EXISTS** — it fires on any absent period, so a document the OCR merely FAILED on is silently filled from the web instead of being retried or recorded as `missing`. ⚠️ **AND THE REPAIR IS MUCH NARROWER THAN THE PDF INDEX SUGGESTS — a claim first written as "15 reparsable" and corrected the same day.** `documents()` keeps `consolidated == "True"` and nothing else, and **ACB filed no consolidated statement before 2010** (2003/2004/2007/2008/2009 are parent-company-only; consolidated starts 2010 with 5). So `documents("HOSE","ACB")` returns 65 documents covering **2010-2026**, `skip_existing=True` skips all 17 of its years, and **not one of ACB's 27 HTML rows is in a year the parser will open**. VCB has a consolidated filing from 2006, so its `documents()` covers 2006-2026, 4 years re-open (2006-2009, 7 documents) and **4 of its 7 HTML rows get a real retry: 2008Q4 IS, 2009Q1 BS, 2009Q2 BS, 2009Q2 CF** (plus 3 `missing` rows — 2006Q4 IS+CF, 2007Q4 IS). The other 30 rows have no document the parser will open: ACB's 27, and VCB's 2008Q3 where no Q3-2008 filing exists at all. ⚠️ **Reaching ACB's 27 means deciding to accept a PARENT-ONLY statement where no consolidated one exists — a change of which ENTITY the numbers describe, not a parser fix**, and it must be a decision put to the user. ⚠️ **`use_api=False` alone does NOT delete an existing HTML row**: `skip_existing=True` forces `merge=True`, and a merging write only rewrites the quarters the run produced, so a failed retry leaves the `cafef` row in place. **Only an authoritative full run (`skip_existing=False`, non-merging, ~2.4 h/ticker) rebuilds the grid and turns unproduced quarters into `missing`.** ⚠️ **The code argues the opposite of the rule in as many words** — `from_api`'s docstring reads *"This is not a lesser source — for the quarters OCR cannot read it is a BETTER one"* — so the fix is a DECISION to reverse, not a bug to patch: default `use_api=False`, expose no knob that turns it on, and let `missing` be the answer. ⚠️ **CafeF's "not reported" sentinel is a literal `-1`** (`NOT_REPORTED`), which lands as −1 dong in a column of billions and takes part in no subtotal, so no reconciliation catches it. ✅ **The `source` column is what made this auditable and must survive any fix.** CLAUDE.md §6-2-octies, §5 rule 24. | `web_scraper/cafef_financials.py::build/from_api`; `bronze.cafef_financial_reports`; both statement CSV families |
| **SCH-1** | substantive ✅ **FIXED 2026-08-23** | ⚠️ **28 OF THE 30 SINGLE-NAME `unified_schema_*` SCHEMAS WERE STALE, AND THEIR STALENESS DATES ARE A FOSSIL RECORD OF EVERY SCOPED RE-SCRAPE EVER RUN HERE.** Found by `pipeline.freshness` on its first full run (TODO `P1`), 2026-08-23. `MAX(date)` reads 2026-08-21 on the schemas that WERE rebuilt and says nothing about the others, so the 2026-08-23 carry-up (§6-2-ter) looked complete: it rebuilt `all`, `price10k`, `liquid` and `quality` and left every single-name schema behind. The dates split into four groups and each one names a past decision: **5 at 2026-08-19** (FPT, HPG, SSI, STB, VIC — the `SSK-1` single-stock track), **10 at 2026-08-07** (BID, CTG, HDB, MBB, SHB, SSB, TCB, TPB, VIB, VPB — the BANK partition re-scrape), **9 at 2026-06-26** and **4 at 2026-06-25** (never refreshed since the `FRZ-1` freeze). ⚠️ **This is CLAUDE.md §5 rule 14 from the other side** — gold moving does not mark the unified layer stale — and it had never been COUNTED. ⚠️ **Nothing downstream was wrong**: all 28 hold pools only, **0 non-pool tables**, so no `__final__` table or dataset was ever built from one. ✅ **FIXED the same day**: `pool__basic` + `pool__targets` rebuilt for all 28, measured **21 s per schema** end to end (of which the two Dagster steps are 1.7 s; the rest is process start-up), so ~10 minutes for the lot — **28 of 28, 0 errors**, and `pipeline.freshness` now reports `STALE: 0` across all 30 single-name schemas. ⚠️ **The other 23 pools per schema — `pool__bonds` and the 19 `pool__economy_*` — are deliberately NOT rebuilt** and remain on the old calendar, which `status_data` already reports as `pools_behind`; a wide join over one of these schemas would INNER-join back down to theirs. CLAUDE.md §6-2-quinquies. | every `unified_schema_<name>` for a single ticker |
| **DEP-1** | substantive ✅ **FIXED 2026-08-23, same hour it was created** | ⚠️ **A MONITORING VIEW BLOCKED EVERY REPAIR IT EXISTED TO RECOMMEND.** `pipeline.freshness` first shipped `health_schema.ticker_freshness` as a `UNION ALL` **VIEW** over every layer. PostgreSQL records a view's dependency on the tables beneath it, and **every builder in this repo opens with `DROP TABLE IF EXISTS`** — `_ingest_unified_pool_basic` at `preprocessor.py:7994`, and the silver and gold builders the same way. So the first rebuild after installing it died: `psycopg2.errors.DependentObjectsStillExist: cannot drop table unified_schema_vnm.pool__basic because other objects depend on it / DETAIL: view health_schema.ticker_freshness depends on it`. ⚠️ **The blast radius was the whole write path, not one asset** — the same view would have blocked the `gold.stocks_ta` rebuild (`STA-1`) that had run hours earlier, and every future carry-up. **A monitor that must be uninstalled before the system can be repaired is worse than no monitor.** ✅ **FIXED by making all three objects `plpgsql` FUNCTIONS**: a function body is not parsed for dependencies, so `DROP TABLE` is unaffected (verified directly — a table created and dropped with the functions installed). Two things fell out for free: the layer list is now discovered at CALL time (a schema built later needs no re-install, where the view had to freeze its list into a `COMMENT`), and the layer filter is an ARGUMENT rather than a predicate the planner has to prune. ⚠️ **The cost is that `ticker_freshness(NULL)` walks every layer** — a `WHERE layer = 'x'` AFTER the call cannot push into the function, so it takes 32.9 s where `ticker_freshness('silver')` takes 0.25 s. **Pass the layer, do not filter the result.** `pipeline/CONTEXT.md` §1a-bis. | `health_schema.*`; anything that DROPs a table a monitor reads |
| **BKT-1** | minor, MEASURED | ⚠️ **THE BACKTEST REBALANCES ON A CALENDAR GRID BUT SCORES EACH PICK WITH A ROW-STEPPED RETURN, SO A NAME WITH A TRADING GAP IS CREDITED WITH RETURN EARNED AFTER THE BOOK WOULD HAVE SOLD IT.** `portfolio.rebalance_dates` takes every `h`-th DATE of the panel, while `pool__targets.return_{h}day` is `close_adjust` shifted `h` ROWS within the ticker's own series — verified 2026-08-20 to **8.9e-16 over 2,373,356 rows, 100 % exact within 1e-9**. For a name that trades every session the two windows coincide; for a name with a halt or a gap, `h` rows reach a LATER calendar date than `h` sessions. **Measured: 4.2 % of picks at h=20 (100 of 2,360) and 2.3 % at h=10 (108 of 4,720)**, with a worst single-name disagreement of **28.6 pp**. ⚠️ **At portfolio level it is immaterial and it favours the SKEPTIC**: recomputing all 118 h=20 periods on the calendar window gives Sharpe **+2.0058 against the published +1.9913** (−0.015), and h=10 **+2.5686 against +2.5310** (−0.038) — so every published figure is slightly CONSERVATIVE, and no conclusion moves. Recorded rather than fixed because the fix is a choice, not a repair: either step the return on the calendar (correct for a portfolio, and requires re-deriving every `return_{h}day`) or rebalance on each ticker's own rows (impossible for a portfolio, which trades on one date). ⚠️ **Found by `walkforward.pair`'s reconciliation, which exists to catch exactly this**: on names trading every session its daily reconstruction reproduces `return_{h}day` to **1.22e-15**. | `backtest/portfolio.py::rebalance_dates` vs `pool__targets.return_{h}day` |
| **WFO-1** | ✅ **FIXED 2026-08-21** | ⚠️ **`walkforward` DERIVES ITS OUTPUT PATH FROM NOTHING THAT DISTINGUISHES TWO EXPERIMENTS, SO A SECOND SWEEP SILENTLY DESTROYS THE FIRST.** `run.DEFAULT_OUT` is `results/walkforward/` and the three artefacts — `folds.csv`, `per_fold.csv`, `predictions_oos.csv` — are written by BASENAME, with no term for the table, the target, the horizon or the config. So `RUNBOOK.md` §3's documented command, run at a second horizon, overwrites the previous horizon's entire out-of-sample track with no prompt, no fingerprint check and no message. Found 2026-08-20 while running the h=10 walk-forward: the h=20 track (`PRF-1`, 118 periods, the strongest evidence in the repo at the time) was one omitted `--out` away from being destroyed, and `predictions_oos.csv` is 16.5 MB of model output that costs 33 minutes of GPU to regenerate. ⚠️ **This is the third instance of one shape**: `PRF-8`'s concurrency trap (`walkforward/CONTEXT.md` §8c — dataset folders named from the DATA with no term for which process built them) and `PRB-1` (a probe run merged into the chain's report root because only the ROOT separates them). `namespace_lock` was the fix for the first and a second report root for the second; neither helps here, because a sequential re-run of the same command is not a concurrency problem. ⚠️ **The `evaluate` side has the mirror defect**: `--horizon` defaults to 20 and sets both the period cut and the `return_{h}day` column scored, so pointing it at an h=10 track without `--horizon 10` silently scores the wrong label against the right predictions. ✅ **FIXED 2026-08-21 by the REFUSAL, not by the rename.** Deriving the leaf was rejected: five tracks already exist under hand-chosen names that three registers cite by path, and moving them buys a guarantee the refusal gives anyway. `walkforward/manifest.py` writes a `manifest.json` recording the experiment (`ticker`, `table`, `first_test`, `step_months`, `val_months`, `scale_target`, `rank_min_width`) and `run.main` **claims** the directory before a single fold is built, naming the offending field on a mismatch; `--force-out` overrides. ⚠️ **The five LEGACY tracks are covered too** — the table is recovered from `folds.csv`'s run names — but **only the table is**, because `folds.csv` records no knobs and §5 rule 2 forbids inferring an absent measurement. ✅ **The `evaluate` mirror defect is fixed in the same commit**: `evaluate` and `compare` DERIVE the horizon from the track and raise when an explicit `--horizon` disagrees, and `compare` additionally refuses arms built at different horizons. ⚠️ **`RPR-1`'s half was fixed beside it**: `results/` had no `.gitignore` negation, so both tracks were untracked as well as overwritable; `folds.csv` + `per_fold.csv` are now negated (26 files, **41 KB**), while `predictions_oos.csv` stays ignored because 18 of them are **323 MB**. Verified: `git ls-files --others --exclude-standard results/` went from 0 files to 26. | `walkforward/manifest.py`, pinned by `walkforward/test_manifest.py` (14 tests) |
| **FNM-1** | substantive | ⚠️ **THE SELECTION SCORED THE CHANNELS UNDER ONE FEATURE REPRESENTATION AND THE MODEL CONSUMES ANOTHER.** The cross-sectional selection ran with `feature_normalize=cs_rank` — every channel ranked WITHIN each date before the window, which `cross_sectional.py` §3 argues is the representation a cross-sectional model wants because it removes the level that acts as a date proxy and the size that acts as a permanent stock label. `train_test_creator` does no such thing: the dataset records **13 scaled columns, 0 bounded**, standardised GLOBALLY with one `StandardScaler` fitted on the train slice. So the 13 channels were chosen under a per-date rank and the LSTM is fed the raw values. Found 2026-08-18, same family as `RNK-1` but for the FEATURES rather than the label. ⚠️ **What it does and does not damage**: it does NOT invalidate the model's own out-of-sample number — that is measured on its own splits under its own representation — but it does weaken the sentence *"this model is built on a shortlist that cleared z = +9.09"*, because that bar was measured for a representation the model does not use. CLAUDE.md §5 rule 1 states the principle: **a bar computed for one configuration says nothing about another**. ⚠️ It is not obvious which side should move: adding per-date ranking to `train_test_creator` changes every panel dataset, while dropping it from the selection throws away the argument in `cross_sectional.py` §3. **Measure first** — re-run the selection with `feature_normalize=none` and compare the kept set. | `train_test_creator/dataset.py::build` vs `feature_selection/cross_sectional.py::CrossSectionalSelector._design` |
| **FRZ-1** | substantive ✅ **FIXED 2026-08-23** | ⚠️ **THE PRICE UNIVERSE HAS BEEN FROZEN SINCE 2026-06-26 FOR 755 OF 781 TICKERS, AND `MAX(date)` HIDES IT COMPLETELY.** Measured 2026-08-19 while auditing for the single-stock h=10 track. `silver.stocks_basic` reports `MAX(date) = 2026-08-07` and every downstream freshness check that reads one scalar reads that number — but the max per TICKER splits: **20 tickers at 2026-08-07** (exactly the 20 GICS 401010 banks, i.e. the BANK partition's scoped re-scrape), **4 at 2026-07-08**, **601 at 2026-06-26**, **154 at 2026-06-25**, 2 older. So the whole non-bank universe — HPG, FPT, SSI, VNM, VIC, MSN and 749 others — has carried no new price for ~37 sessions, and nothing raised. This is CLAUDE.md §5 rule 10 exactly, and it is already documented for `gold.forex` ("29 of 357 in Aug, 328 at 2026-06-08/09"); what is new is that **the same defect sits on the stock price table, which is the source of every chain in the repo**, and had not been written down. ⚠️ **A scoped re-scrape FIXES one ticker and BREAKS its `pool__ta` join** — see **STA-1**: refreshing HPG/SSI/FPT/VIC/STB to 2026-08-19 on 2026-08-19 (19m29s for five, 0 ERROR) moved `pool__basic` 37 sessions past `gold.stocks_ta`, which is stuck at 2026-06-26 for 775 of 777 tickers, so a `basic + ta` INNER join now truncates where before the two happened to end on the same day. **The freshness of a ticker is not a property of the ticker; it is a property of the last scrape's SCOPE.** The check is one query — `SELECT max(date), count(*) FROM (SELECT ticker, max(date) AS max_date FROM silver_schema.stocks_basic GROUP BY 1) t GROUP BY 1` — and it belongs in `pipeline.status_data` beside `pools_behind`. ⚠️ **RE-MEASURED 2026-08-22 and it has got WORSE in the one way that matters, because the scoped re-scrape moved the scalar again**: `MAX(date)` now reads **2026-08-19 and FIVE tickers produce it** (the `SSK-1` five), with 19 at 2026-08-07, 4 at 2026-07-08, **599 at 2026-06-26** and 152 at 2026-06-25 — **757 of 781 stale**. Names per session fall **779 → 627 → 28 → 24 → 5**, so the tradeable cross-section ends **2026-06-25** while the table's one freshness number has advanced by six weeks. **Every scoped re-scrape makes the scalar more wrong**, which is the argument for TODO `P1` (a per-ticker freshness view) rather than another scalar.  ✅ **FIXED 2026-08-23 — and the fix was a SCRAPE MODE, not a bigger scrape.** The reason this sat open for two months is that the scraper could only refetch from 2009: **615 s per ticker** for all four tabs, i.e. **~67 h** for the universe at the then-current `SCRAPER_MAX_WORKERS = 2`. `_scrape_tab` now takes `incremental=True`, which resumes each CSV from **its own last date** and merges — **2.9-5.2 s** per stale ticker, and the resumed file reproduces a full scrape **cell for cell** (PNJ, 4,344 × 12, 0 differing cells). With workers raised 2 → 12 on a measurement (per-ticker cost is FLAT at 1/8/16 concurrent, so CafeF was never throttling) the whole universe took **1h 05m, 0 errors, 780/780 on every tab**. Result: **771 of 784 tickers at 2026-08-21** against 5 producing the max date, `silver.stocks_basic` 2,389,137 → **2,428,227** rows, and the cross-section holds **771-783 names on EVERY session** — the `779 → 627 → 28 → 5` cliff is gone. The 13 remaining stragglers carry **SEVEN distinct dates**, largest group **5 on 2026-07-08** (IHK 05-21, DDG 06-23, VNE 06-26, SSN/STL 07-06, DSE/KOS/SIP/VPI/DZM 07-08, TCD/VE2 07-14, CYC 07-30), the signature of individual delistings rather than a scrape failure, which clusters on one date. ⚠️ **This line read "13 DISTINCT dates" until 2026-08-23 and its own list disproved it** — the prose counted tickers. Found by `pipeline.freshness` on its first run (TODO `P1`); CLAUDE.md §6-2-bis carried the same wrong number. ✅ The conclusion survives and was re-verified another way: each of the 13 raw CafeF price CSVs ends on exactly the date silver holds, so the incremental scrape did attempt them and the source returned nothing after. ⚠️ **The mode is only safe because of its RESTATEMENT GUARD, which fired on 304 of 780 price tickers (39 %)**: `close_adjust` is re-based across the whole history by any split or dividend, so a naive append splices two price bases into one series — invisible to every freshness check, since the row count, the date range and the last date are all correct. The resume refetches a 45-day overlap, compares it cell-by-cell, and falls back to the full refetch on disagreement; it fired on **0** of the three tabs with no adjusted column. ⚠️ **This closes P1 and NOT P2** — `gold.stocks` and `gold.stocks_ta` were already 30 and 54 sessions behind and this scrape widens that gap. ✅ **`P1` (a per-ticker freshness view) CLOSED 2026-08-23** — the scalar `MAX(date)` was as wrong the day this was fixed as the day it was found, so `pipeline.freshness` now reports a distribution, ages every layer against the price spine's calendar rather than its own, and tells a scrape SCOPE (a cliff on one date) from delistings (scattered). Its first run found **28 of the 30 single-name unified schemas stale** in four layers matching every scoped re-scrape ever run here (5 at 2026-08-19, 10 banks at 2026-08-07, 9 at 2026-06-26, 4 at 2026-06-25) - tracked as **SCH-1**. CLAUDE.md §6-2-quinquies. CLAUDE.md §6-2-bis; `web_scraper/CONTEXT.md`. | `silver.stocks_basic`; `pipeline.status_data`; every `pool__*` built from it |
| **STA-1** | substantive ✅ **FIXED 2026-08-23** | ⚠️ **`gold.stocks_ta` ON DISK WAS NOT BUILT BY THE CURRENT BUILDER, AND NOTHING SAYS SO.** Found 2026-08-16 while tracing the 1e9 unit bug. It carries **13 legacy short column names** — `val_matched_bn`, `val_negotiated_bn`, `vol_matched`, `vol_negotiated`, `f_buy_val`, `f_sell_val`, `f_net_val`, `f_buy_vol`, `f_sell_vol`, `f_net_vol`, `own_pct`, `foreign_room`, `volume` — and **zero** of `silver.stocks_basic`'s own names. No Python in the repo produces any of them: `_ingest_gold_stocks_ta` carries silver's names straight through, and its sibling `gold.stocks` (rebuilt more recently) has the silver names and none of the legacy ones. Three independent signatures agree it predates the 2026-08-03 split: **2,678,167 rows against silver's 2,388,975** (289,192 MORE, i.e. a different universe or dedup), `MAX(date)` **2026-06-26** against silver's 2026-08-07, and the naming itself. ⚠️ **The consequence is that "rebuild `gold.stocks_ta`" is not a maintenance action** — it renames 13 columns, drops ~289 k rows and adds 42 days, and `pool__ta` inherits every bit of that, so any shortlist, `__final__` table or fingerprint naming `val_matched_bn` / `f_net_val` breaks the moment it runs. ~11 GB and hours of compute. It is therefore its own decision, not a step inside someone else's fix — which is exactly why the 1e9 correction below was shipped as a code fix WITHOUT the rebuild.  ✅ **FIXED 2026-08-23 — rebuilt, and all three signatures are gone.** `gold.stocks_ta` is now **2,428,227 rows over 784 tickers to 2026-08-21**, matching `silver.stocks_basic` EXACTLY (was 2,678,167 / 777 / 2026-06-26), with **0 of the 13 legacy names** left and silver's own names carried through across 946 columns. All five `pool__ta` partitions (`all`, `vcb`, `bank`, `vn30`, `acb`) were rebuilt with it, each carrying 0 legacy columns, and **the `basic + ta` INNER join now returns exactly `pool__basic`'s row count on every universe** — which was the actual damage, since the two ended on different days and the join silently truncated. ✅ **It also closes `SKW-1` as a number**: on VCB's 4,276 shared stock-days the two tables disagree on `value_matched` for **0 rows**, where before the same stock-day gave two answers (`pool__ta` carried pre-`OUT-1` flow values). ⚠️ **The blast radius was MEASURED before the rebuild rather than assumed, and it was smaller than this entry feared**: querying `information_schema` for the 13 names showed the headline cross-sectional chain (`rank_20day__final__d20_h20`, `rank_10day__final__d20_h10`, both `__wide`) names **none** of them, and exactly **two** artefacts break — `pool__shortlist__return_5day__d20_h5` (2) and `return_5day__final__d20_h5` (1), both in the VCB `return_5day` chain CLAUDE.md already marks *"do not quote it"*. Those two are stale now and must be rebuilt before reuse. ⚠️ **"~11 GB and hours of compute" WAS WRONG BY AN ORDER OF MAGNITUDE — it took 40 minutes** (10:49 → 11:29, 784/784 tickers, 0 errors) and disk did not move measurably. That estimate sat in this entry since 2026-08-16 and is the reason the item was deferred for a week: **an unmeasured cost deterred the work it described.** CLAUDE.md §6-2-quater. | `gold.stocks_ta` vs `preprocessor::_ingest_gold_stocks_ta`; `pool__ta` on all 3 universes |
| **OUT-1** | substantive | ⚠️ **ONE CORRUPT SOURCE CELL IS ENOUGH TO MANUFACTURE A FINDING, AND IT DID.** `silver.stocks_basic`, **VCB 2026-01-05**, carries `prop_buy_val = 4.001e17` — 400 quadrillion VND — against that day's entire turnover of **2.06e11**, on `prop_buy_vol = 697,000` shares at a close of **57,100**: an implied **5.7e11 VND per share**, ten million times the real price. Found 2026-08-16 while probing the new `pool__basic` derived block for forward-looking correlation. That single cell drives `corr(drv_prop_net_value_ratio, drv_prop_participation)` to **exactly +1.0** — two channels that are not collinear by construction — and manufactures a **+0.266** correlation against the forward 5-day return on 878 rows. Delete the row and both collapse. ⚠️ **It is not a unit problem and not confined to prop**: implied price on the normal rows is **87,596** against a `close_raw` of 87,500, and the foreign block matches too, so the scale is right. But market-wide **77 of 73,044 `prop_buy_val`** and **1,182 of 1,240,032 `foreign_buy_value`** rows exceed **ten times their own day's turnover** (~0.1% each) — `foreign_*` carries the identical defect, and it is 74% covered where prop is 3%. ⚠️ **Deliberately NOT winsorised in the derived expressions.** Cleaning belongs to the layer that owns the column; clipping a source value inside a feature is how a data defect stops being visible. The defect is upstream of every ratio built on these columns — including `pool__ta`'s `foreign_net_val_ratio` and `foreign_buy_pressure`, which have never been checked for it. **Anything selecting on a foreign or prop channel should look at its extremes first.** ⚠️ **PARTLY FIXED 2026-08-16 — `_helper_screen_flow_outliers` in the silver ingest.** A flow value/volume pair is NULLed (never winsorised: the corruption factor is not constant — 1e7 on VCB, 1e8 on HPG/TPB — so there is nothing to divide out and the true value is simply unknown) when it fails any of THREE rules, and it took three because each of the first two has a blind spot the next one covers: (1) **implied price** `|value|/|volume|` off from `close_raw` by >100x — thresholds read off the distribution, where **99.5% of flow rows sit within 2x, 99.8% within 10x, 99.98% within 100x**; (2) **flow volume** >100x the day's total, which catches rows where value AND volume are corrupt together so the implied price looks fine (`STB 2025-12-30` carries **1.5e13 shares**); (3) **flow value** >100x total turnover in VND, the only rule that works when the volume is NULL or zero — `SHB 2025-10-30` survived rules 1 and 2 that way and left `drv_prop_participation` with a maximum of **57,644** against a p99 of 0.269. Result: **611 of 2,388,975 rows (0.0256%)** NULLed, row count unchanged, and on VCB `corr(prop_net, prop_participation)` **+1.000000 → +0.3270** with the forward-return correlation **+0.2658 → +0.0280** (Spearman −0.0049). ⚠️ **TWO CLASSES REMAIN, REPORTED AND NOT SCREENED**, because for both the source of the inconsistency is undetermined and deleting on a guess is worse than carrying it: **2,844 pairs hold a real volume with a ZERO value** — MIXED, not the rounding artifact an earlier note claimed, since 362 of 857 `prop_sell` rows imply <100 M VND but **305 imply ≥1 BN**, the largest 1.73e13 (VCB 2026-01-05 is one: buy pair NULLed, sell pair survives as `val=0, vol=165,300`) — and **196 rows carry flow volume on a day with no traded volume at all**. ⚠️ A separate defect found in the same pass and fixed with it: the derived flow ratios divided by MATCHED turnover, but flow trades in the negotiated channel too (`ABB 2026-06-26`: 19 bn matched against **393 bn negotiated**), inflating those rows ~20x; the denominator is now matched + negotiated, taking `drv_foreign_net_value_ratio` from **[−239.6, +75.0]** to **[−4.87, +2.27]**. ⚠️ `gold.stocks_ta` / `pool__ta` were NOT rebuilt (see **STA-1**), so they still carry the unscreened values. | `preprocessor::_helper_screen_flow_outliers`; `silver.stocks_basic` ← `bronze.cafef_prop_trading` / `cafef_foreign` |
| **PRB-1** | ✅ **FIXED 2026-08-19** | ⚠️ **A PROBE RUN MERGED INTO THE CHAIN'S REPORT ROOT IS TREATED AS A CHAIN INPUT, AND ONE OF THE TWO WAYS IT FAILS IS SILENT.** `final_features.plan_from_reports` groups every run under a root by `(schema, target, SETUP_KEYS)` and builds one table per group. **The DATA WINDOW is not a setup key.** So `PRF-7`'s pre-2017 probe — deliberately run on 44 % of the dates to bound the selection look-ahead — grouped with the two full-sample runs and would have unioned its 15 channels into `rank_20day__final__d20_h20` on the next rebuild, **reporting success**. `FNM-1`'s `feature_normalize=none` probe differed on a setup key instead, so it produced the LOUD failure: two groups wanting one table name, and `plan_from_reports` raised — which stopped `final_features` planning *anything*, including the unrelated `h=10` chain. ⚠️ **`--scope` cannot fix either**: the table name is `(schema, target, lookback, horizon, scope, shape)`, so a scope suffixes BOTH groups identically. ⚠️ **No number was wrong**: the table on disk names its own 2 source runs and 13 channels (`obj_description`, fingerprint `d21d7537456d`) and predates both probes — it is the NEXT rebuild that would have been. **Fixed** by moving both probes to `reports/feature_selection_probes/` and pointing the two probe jobs' `results_into` + `REPORT_ROOT` there, so a re-run cannot recreate it; the reasoning is recorded in `kaggle_config.json`'s `_probes_note`. ⚠️ **The general rule: a run that measures the SELECTION is not a run that feeds the CHAIN**, and only the root separates them. | `final_features/builder.py::plan_from_reports`; `kaggle_gpu/kaggle_config.json` |
| **VRM-1** | ✅ **FIXED 2026-08-20 (the SHAP half)** | ⚠️ **THE WIDTH CEILING ON A T4 IS VRAM, NOT HOST RAM, AND IT IS `xgb_shap`'s SHAP CONTRIBUTIONS.** Measured 2026-08-19 (`PRF-9` pilot): at **140 channels** over 624,238 rows the host peaked at **24.5 GB and survived**, while XGBoost died in `XGBoosterPredictFromDMatrix` — *free 3.00 GB, requested 3.15 GB* on a 14.6 GiB T4. The allocation is `(n_rows, channels × 6 + 1)`, i.e. **linear in exactly the width a widening increases**. ⚠️ **`MEM-1`, `P3-2` and `PRF-9` all predicted the HOST half and all three were wrong about which wall binds.** ⚠️ **`selector._tick` cannot see it**: it reports torch's VRAM (6.2 GB at the failure) while XGBoost allocates through its own CUDA allocator, so ~11.6 GB was in use and nothing said so — the same shape as P1-4b's *'nothing in the run said how much memory it was using'*, one library out. **Measured ceiling: 120 channels COMPLETE in 32.6 min; 140 fails.** Host model, fitted on two points: `peak_GB ≈ 1.54 + 0.164 × channels`. Not fixed — the candidates are chunking the SHAP predict the way `P1-4` chunked the ranker, running `xgb_shap` on the host (~15× slower per `gpu.py` §1), or dropping it from `METHODS` (⚠️ §19 measured `permutation` as the only load-bearing member, so `xgb_shap` may be affordable to lose — untested). | `feature_selection/gpu.py`, `selector.py::_tick`; measured in the `cross-sectional-wide-pilot` job | ✅ **RESOLVED 2026-08-20 for the allocation that actually killed it.** `gpu.tree_shap` now calls `pred_contribs=True` in ROW BLOCKS sized to ~512 MB (`gpu.shap_row_chunk`) and accumulates `sum|contribution|` in float64, dividing by `n` at the end. ⚠️ **It is EXACT, not an approximation** — a SHAP contribution is per-row independent and the statistic wanted is a column MEAN, so the blocked path returns the same number the single allocation would. Verified at **max abs diff 0.000e+00** against the unblocked path at 20 / 140 / 300 design columns, including a deliberately awkward block size of 997 rows. At 140 channels the panel design is 841 columns, so the old single allocation was **2.10 GB** on 624,448 rows and the blocked one is **537 MB** — the run died asking for 3.15 GB with 3.00 GB free. ⚠️ **THE CODE IS FIXED; THE CEILING IS NOT YET RE-MEASURED.** The SHAP step is no longer width-bound, but VRM-1's own history records three predictions about which wall binds (`MEM-1`, `P3-2`, `PRF-9`) and **all three were wrong**, so what binds at 233 channels is an open measurement — the `cross-sectional-wide-h10` job is it. The row stays struck-through rather than moved to Resolved until that run reports.
| **FLT-1** | substantive | ⚠️ **THE TRADINGVIEW FOREX BROKER FILTER SILENTLY FAILS FOR 19 OF 47 BROKERS, AND IT FAILS OPEN.** `build_forex_link_scrape_actions` injects the source into `selectedSearchSources["forex"]`; for **27 brokers the links CSV comes back with exactly one exchange** and for **19 it comes back with 49**, dominated by `FX_IDC` — TradingView's unfiltered default. `ibroker` enumerates 0. Nothing raised for months because failing open still returns thousands of rows, so every count looks healthy and the emptiness guards never fire. The signature is unmistakable once counted (2026-08-14): six brokers returned **byte-identical ~16,700-symbol lists** (`velocity_trade` = `wh_selfinvest` = 16,748; `interactive_brokers` 16,727; `osmanli_fx` 16,718; `phillip_nova` 16,716; `trade_nation` 16,674) and eight more shared one 8,48x-row list, against a normal broker's 48–169. ⚠️ **The damage lands in `data/`**: the adder fetches whatever that broker's links CSV lists, so before 2026-08-14 `capital_com/` held `ACTIVTRADES_*` and `SAXO_*` files and eight folders held one identical 50-file set — **the folder name tells you nothing, the filename does**. Contained rather than fixed: `parameters.data_only` keeps the fetch on the 27 that work while links still enumerate all 47. **37 of 47 brokers' own books remain unreachable until this is fixed.** | `web_scraper/trading_view_scraper.py::build_forex_link_scrape_actions` |
| **SHP-1** | substantive | ⚠️ **THE FOREX SCRAPER WRITES TWO FILE SHAPES AND ONLY ONE WAS EVER INGESTED — 71% OF THE FOLDER WAS READ AND SILENTLY DISCARDED.** `_scrape_data_trading_view_link` detects OHLC per series and writes either `(date, open, high, low, close, volume)` or `(date, value)` — **4,402 files against 1,787** on 2026-08-14. Every clean layer in `_ingest_bronze_forex` filters on `value`, so the unbatched `pd.concat` of all files produced a `value` column from the OTHER files and `REMOVE_RECORD_IF_COLUMN_IS_NULL("value")` then dropped every OHLC row **without a word**. That is why bronze held 357 series while the disk held 3,129. **Fixed 2026-08-14**: `value` is coalesced from `close` before the clean, taking bronze 357 → **3,129 series** and 1.4 M → **13.7 M rows**. ⚠️ **The fix is justified by the extraction code, not by overlap** — no series on disk carries both shapes, so nothing could be compared; the scraper's `bulk_js` pushes `[date, v[1]…v[5]]` when a series is OHLC and `[date, v[4]]` when it is not, i.e. **the same slot 4 of the same array**, labelled `close` in one branch and `value` in the other. ⚠️ It only surfaced because batching turned the silent drop into a `KeyError` on the first batch containing no value-shaped file. **The sibling ingests (`bonds`, `funds`, `economy`, `indices`) have the same `value`-only filter and have never been checked for this.** | `preprocessor::_ingest_bronze_forex`; `trading_view_scraper.py` `bulk_js` |
| **EVD-1** | substantive | Nothing upstream ever cleared a null. **Partly measured 2026-08-09**: the bare `pool__basic` run was reproduced bit-identically and given the 20-draw null it never had — it **fails at `z = +1.46`** (§10b), so the archive now reads `no_null=18, failed_null=2` instead of `no_null=19, failed_null=1`. **A THIRD measurement on 2026-08-10 CLEARS its p95 at `z = +2.15`** (§10d) — on 31 more sessions and the measured cut, so a third procedure rather than a third trial. It is recorded as `evidence=cleared_p95_not_a_pass`: `p = 0.0476` is the `1/(n+1)` floor, the 20 draws are fat-tailed (−0.0705 … +0.0648, mean +0.0013), and **the model trained on it shows no skill** (test IC −0.0345 vs bar +0.1348). The remaining 18 are not in that price class: `basic+economy_usa` alone spends **12,255 s on `permutation` per pass** at 1,458 channels, so one 20-draw null is ~68 CPU-hours and all 18 is over 1,000. **Still not fixable downstream** — everything below inherits it. Next lever is §12c (`permutation_repeats` 10→3, CUDA, 10 draws), with the observed re-run at the same settings per §8. | `feature_selection/CONTEXT.md` §14b, §10b, §10d |
| **SKW-1** | substantive | **The five `unified/pool__*` assets do not move together, and only one thing looks.** Materialising `pool__basic` + `pool__targets` for VCB on 2026-08-10 took them to 4,266 rows / 2026-08-07 and left `pool__ta`, `pool__fa` and the 19 `pool__economy_*` at 4,235 / 2026-06-25. Harmless for a `--scope basic` build, which joins only the two. **Not harmless for a wide one**: `final_features` joins INNER across pools, so rebuilding `return_5day__final__d20_h5` would truncate to the laggard's calendar and the table would look unchanged — 4,235 rows, same as before, with a `pool__basic` that has moved 31 sessions past it. `pipeline.status_data` now reports a `pools_behind` count; it does **not** fail the stage, because failing would make `--apply` re-materialise `pool__ta` (hours, ~11 GB upstream) for a chain that never reads it. The real fix is a freshness assertion in `unified/pool__*` itself. ⚠️ **DELIBERATELY WIDENED 2026-08-16, and it is now a CONTENT skew and not only a calendar one.** `silver.stocks_basic` was rebuilt with the OUT-1 flow screen and `pool__basic` re-materialised on all three universes; `pool__ta`, `pool__fa` and the 19 `pool__economy_*` were **not**, because `pool__ta` cannot be rebuilt without triggering **STA-1** (13 column renames, ~289 k rows, hours). So on every universe `pool__basic` now holds screened flow and `pool__ta` holds the unscreened original — the same stock-day, two answers, and a join of the two carries both. The two are not in conflict about any column they SHARE (the screen NULLs source columns, and `pool__ta` re-derives its own), but a reader treating `pool__ta.foreign_net_val_ratio` and `pool__basic.drv_foreign_net_value_ratio` as the same measurement is wrong twice over — different denominator AND different screening. | `pipeline.status_data`, `orchestration/assets/unified.py` |
| **NUL-3** | substantive | ⚠️ **THE EVALUATOR'S PANEL NULL IS NOT LABEL-NEUTRAL — ITS CENTRE DEPENDS ON THE SCORE VECTOR**, so it manufactures clears for some models and hides them for others. Measured on the bank panel 2026-08-10: the same 200-draw null centred at **−0.0171** for `baseline_ar`, **+0.0076** for `baseline_ridge_flat` and **+0.0109** for `baseline_ridge_stats`. A null that shuffles outcomes against a fixed score should centre near zero regardless of which score it is handed. The consequence is not academic: `baseline_ar` scored `ic=+0.0056` against a **negative p95 bar of −0.0059** and was reported as CLEARING at `p=0.005`, while its honest daily-IC t-stat is **+0.23**. Conversely `baseline_ridge_stats`, the highest t on the panel at **+1.15**, FAILED the same null (`p=0.254`). **The suspected mechanism is score structure**: AR predicts from `close_adjust`, a price LEVEL that is near-static per ticker, so its score is close to "which bank is this" and the date-block shuffle interacts with it systematically. Until this is fixed, **quote the daily-IC t-stat on a panel, not `ic_clears`.** | `result_evaluator.metrics.evaluate_panel`; 3 bank runs' `ic_null_mean` |
| **NUL-1** | substantive | The evaluator's null shuffles outcomes against a *finished* score vector, so it prices in nothing about feature selection, architecture search or early stopping. It can kill a run, never confirm one. Structural — the evaluator cannot re-run the selection. | `result_evaluator/metrics.py` §3b |
| **DRF-1** | substantive | **Worse after the STL-1 rebuild**: 126 of 721 VCB channels put >1% of the test set beyond 5 train-sigmas and **18 put 100% of it there** (was 48 and 4 at 203 channels). The tail is also far more extreme than the old "+9 to +13 sigma" — `european_union__…__euestr` sits at **+885 sigma** and `usa__…__resppllopnww` at **−282**. Monotone macro levels. Reported in `drift.csv`, acted on by nothing. The fix belongs upstream — differenced channels, not a silent drop here. | `train_test_creator` → `drift.csv` |
| **COV-1** | substantive | Channels were *selected* despite barely existing. **Far wider than first recorded**: 26 on the VCB table have zero TRAIN coverage (all four `prop_*` plus 22 late-starting macro series), and across the archive **248 of 952 shortlisted rows — 26% — sit below 0.95 coverage**, the worst at **0.024**. **Partly addressed 2026-08-09**: `outstanding.csv` now carries `coverage` + a `PARTIAL` flag (§14d), so the fetch list states the risk instead of a later stage discovering it. It **flags, it does not filter** — the archive cannot see where the train/test cut falls, and dropping 248 rows would change the fingerprint set and trigger the STL-1 domino. The defect itself is unchanged: the selection still ranks these channels on the fraction of history where they exist. | `train_test_creator/dataset.py::_screen`, `feature_selection/outstanding.py` |
| **RPR-1** | reproducibility | `src/train_test_set/` and `src/model/runs/*/` are git-ignored (they are large). A fresh clone has no datasets and no runs. Most project history is re-derivable, not reproducible. A design trade-off, not a bug — recorded so it is a choice rather than a surprise. ⚠️ **It stopped being hypothetical on 2026-08-10**: the 29 pre-existing run folders were **deleted on request**, and because they were git-ignored they are gone for good. That includes the 27-run lookback sweep behind `CLAUDE.md` §2a and the two 2026-08-09 runs behind §6 — the only panel-grain run among them. Their METRICS survive as text in `model/CONTEXT.md` §10, in the hub, and in `index.csv`'s git history; **not one of them can be rescored or re-verified again**. `src/train_test_set/` likewise holds exactly one dataset. Treat §10's research log as a citation without its evidence. | `.gitignore`; `runs/` holds 7 |
| **PNL-2** | ✅ **FIXED 2026-08-19** | ⚠️ **THE SELECTION STAGE PICKS SERIES-VS-PANEL FROM THE TARGET'S NAME, NOT THE PANEL'S SHAPE** — `cross = target.startswith("cs_")` (`feature_selection/run.py`). So `--ticker ALL --target return_5day` runs a **781-ticker, 2.39 M-row panel through the single-series path**: one pooled Spearman over `N × T` rows, row-block CV that splits mid-day, and `n_eff = n/h` counting 781 stocks on one Tuesday as 781 observations instead of one. It does not warn — the only guard is the reverse direction (`cs_` target on a 1-ticker panel raises). ⚠️ **This is PNL-1 one stage earlier, and PNL-1's resolution is the fix that was never applied here**: the scorer detects grain from the `ticker` column, *never* a config flag or a name. Found 2026-08-16 when a cross-sectional sweep was requested on `unified_schema_all`. | `feature_selection/run.py:163` | ✅ **RESOLVED 2026-08-19.** `run.resolve_grain(target, n_tickers)` splits the one flag into two: **`derives_rank`** (the target — only a `cs_` label needs `cs_rank_{h}day` DERIVED, and a panel cannot be consulted before it is read) and **`cross`** (the panel's own ticker count — more than one ticker on a date IS a cross-section, whatever the label is called). ⚠️ **Verified not to reinterpret history**: of 33 archived runs, 3 are `all` + a `cs_` target (panel before and after) and 30 are single-ticker `vcb` (series before and after), so no archived number changes meaning. 4 tests in `feature_selection/tests/test_contract.py`.
| **CSP-1** | substantive | ⚠️ **A CROSS-SECTIONAL SELECTION CAN READ EXACTLY ONE POOL, SO NO LAYER-1 SWEEP IS POSSIBLE ON A UNIVERSE.** `read_universe_panel` joins `pool__basic ⋈ pool__targets` in hand-written SQL, and `run_selection` **raises** on any other `--pools` value for a `cs_` target. The consequence: the whole two-layer architecture of §3c — N layer-1 runs over `pool__basic + <each macro pool>`, then one layer-2 run over the survivors — **exists only for single-ticker studies**. `unified_schema_all` carries 23 pools including `pool__ta` (922 cols) and 19 economy pools; a cross-sectional run can see **none of them**. Since §2b's ladder makes width the only thing that has ever cleared a null, the one grain that works is also the one that cannot be swept. Found 2026-08-16. | `feature_selection/cross_sectional.py::read_universe_panel`; `run.py:176-186` |
| **MEM-1** | substantive | ⚠️ **THE WINDOWED DESIGN IS BUILT DENSE AND WHOLE, SO THE UNIVERSE PANEL DOES NOT FIT IN RAM.** Measured 2026-08-16 at 90 channels × 6 stats: **4.03 GB per million rows**, exactly linear (0.70 GB at 174,275 rows; 1.40 GB at 348,440). `unified_schema_all` at 2,388,975 rows therefore needs **9.6 GB against 7.1 GB free** of 15.6 GB total, and `panel_window_design` peaks near 2× because it holds every per-ticker block *and* the `pd.concat` result. Build time is ~2.8 min per design, and a null re-runs it per draw — 21 builds ≈ 1 h of pure windowing before any ranker. ⚠️ **Partially mitigated the same day, NOT closed**: `--design-dtype float32` halves it to ~4.8 GB and is recorded as a SETUP key so the two dtypes cannot union into one table — but it is **UNVERIFIED** (the float64-vs-float32 agreement probe was killed mid-read and produced no output), and it does not address the 2× concat peak, the absence of chunking, or the fact that the largest training fold (~1.9 M × 540 float32 ≈ 4.1 GB) still exceeds the RTX 3050's **4 GB of VRAM**. ⚠️ **AND THE OTHER HALF IS NOT THE DESIGN AT ALL — measured on a Kaggle T4, 2026-08-17.** The 1.25 M × 104 top-300 panel built its design **fine** (189.3 s, ~29 GB of host RAM), then OOMed in `gpu.spearman_vector → _average_ranks_torch → torch.sort`: *"tried to allocate 4.98 GiB, 3.86 GiB free, 10.70 GiB in use of 14.56"*. That function holds `values` + `filled` + a mask — 10.58 GiB at ~536 float64 columns × 1.247 M rows — before `torch.sort` asks for its own output plus an int64 `order`, roughly another 10 GiB. **So the RANK STEP needs ~4× the design in VRAM, and it is a separate defect from the dense design this row was opened for**: no card this side of an A100 80 GB runs it as written. The fix is exact rather than approximate — **rank in COLUMN BLOCKS**, since ranks are per-column independent, so chunking changes no number, unlike `float32` (P0-3: a 52% relative change in `ic_mean`). ✅ **SHIPPED 2026-08-18 (TODO P1-4)** — `rank_block_columns` + a blocked `_average_ranks_torch` + a blocked `_spearman_vector_cuda`; blocked equals dense at **0.0** and the 30-name smoke run reproduces exactly through 2 blocks. On the T4 that step went from an OOM to **12.3 s**. ⚠️ **AND THE NEXT PHASE THEN KILLED THE KERNEL, WHICH RETURNS THIS ROW TO ITS ORIGINAL SUBJECT.** `rank (the ensemble's methods)` died with `DeadKernelError: Kernel died` and no traceback — a cgroup SIGKILL, not a catchable CUDA error — so the binding constraint is now HOST RAM, i.e. the dense design this row was opened for. ⚠️ Nothing in the run said how much it was using, so `selector._tick` now prints `rss=… vram=…` per phase: measured 0.8 → **2.3 GB** on 48,521 rows, peaking at `stability`. Extrapolated at 25.7× the rows that is **~39 GB against ~29-30 GB**, so the panel needs a ~2× reduction or a streaming design, not a trim. | `feature_selection/windows.py::window_design`; `cross_sectional.py::panel_window_design`; **`gpu.py::spearman_vector` / `_average_ranks_torch`** |

## Resolved (38)

| code | issue and fix | pinned by |
|---|---|---|
| **SCP-1** | ⚠️ **A LOG-ONLY HELPER ASSUMED EXACTLY ONE BOUND PARAMETER AND TOOK DOWN THE BUILD.** `_ingest_unified_pool_basic` described its universe for the log as `predicate.replace('%s', repr(*params))`. That is correct for all three sentinels it was written against — `BANK` binds a GICS code, `VN30` binds a ticker list, a bare ticker binds itself, **one value each** — and raises `TypeError: repr() takes exactly one argument (0 given)` for anything binding **none**. Found 2026-08-22 the first time a FILTER-layer screen was built: a screen's predicate is a self-contained sub-select over `filter_schema.universe__<screen>` and binds nothing, so `unified/pool__basic --partition PRICE10K` died before touching the database. ⚠️ **Nothing was wrong with the data, the SQL or the screen** — a string built for a log line was the only failure, which is the class worth recording: the code that fails was not the code doing the work, and the traceback points at `MaterializeResult`, not at the helper. **Fixed** by `_helper_unified_describe_predicate`, which substitutes `%s` positionally for however many values it is given, including zero. ⚠️ It also silently fixed a second latent case — a multi-parameter predicate would have interpolated only the first value and left the rest as literal `%s`. | `preprocessor::_helper_unified_describe_predicate`; `preprocessor/test_filters.py::test_member_predicate_binds_nothing_and_reads_the_table` |
| **BOO-1** | ⚠️ **THE PAIRED BOOTSTRAP COULD RETURN A p-VALUE OF 2.0.** `pair.block_bootstrap_diff`'s two-sided empirical p was `2 x min(P(x<=0), P(x>=0))`, and `P(x<=0) + P(x>=0)` is `1 + P(x==0)` — so the formula exceeds 1 exactly when the bootstrap produces EXACT ties at zero, returning **2.0** for two identical series. Found 2026-08-21 by the `P1-9` test that compares an arm with itself, in code that had already produced `P2-4`'s published numbers. ⚠️ **It did NOT touch those numbers** (0.0004 / 0.067 / 0.141): two different strategies never tie exactly, so the degenerate branch had no way to fire — recorded anyway, because *"it never fired"* is a fact about the inputs and not about the formula. Fixed by clipping at 1.0. | `walkforward/pair.py::summarise`, `test_compare_sharpe.py::test_two_identical_arms_give_a_zero_difference_and_a_p_of_one` |
| **ICT-1** | substantive | ⚠️ **THE PANEL `ic_t` IS OVERSTATED BY EXACTLY √h, AND IT IS THE COLUMN THE REGISTER TELLS YOU TO READ.** `result_evaluator/metrics.py:479` computes `se = daily_sd / sqrt(len(daily))` — the standard error of the mean daily IC over **`n_dates`** — while line 570 computes `n_eff = n_dates / horizon` correctly and **nothing uses it**. Consecutive daily ICs share `h-1` label days, so they are not independent observations; dividing by `sqrt(n_dates)` prices in the cross-sectional averaging and ignores the overlap. Measured 2026-08-18 on `lstm__all__rank_20day__final__d20_h20`: the row reports **`ic_t = 15.50`** on test where the honest figure is **`+3.47`** — a **4.47× = √20** overstatement, and `ic_se` 0.0056 against 0.0249. ⚠️ **The irony is the damage**: `result_evaluator/index.py:50` says *"`test_ic_t` is THE column to read on a PANEL grain: issue NUL-3 says…"* — so the documented escape from one broken statistic is a second broken one, and every panel run in `index.csv` carries it. ⚠️ The codebase already knows the right formula: `metrics.py:373` names `cross_sectional.DailyICSummary` as using `n_dates/h`, and CLAUDE.md §5 rule 7 states it. **Fix**: one line — divide by `sqrt(n_eff)` — then `python -m result_evaluator --rescore`, which recomputes from `predictions_*.csv` and needs no GPU. It will move every archived PANEL run's `ic_t`, which is the point. ✅ **FIXED 2026-08-18 (P0-7).** `panel_core_metrics` takes a `horizon` and divides the daily-IC sd by `sqrt(len(daily) / h)`; `evaluate_panel` passes its own. The default is `horizon=1` — "no label overlap", which is honest for a caller that does not know its horizon, and is why `panel_null_metrics` may leave it alone (that path reads only `CORE_METRICS`). ⚠️ **`--rescore` ALONE DOES NOT FIX `index.csv`** — it rewrites each run FOLDER's `metrics.{csv,json}` and `verdict.txt`; `index.csv` is written only by **`--rebuild-index`**, so both were run (41.6 s + 42.7 s, no GPU). Measured after: the panel run's `ic_t` **15.50 → +3.47** test and **+4.15** val, `ic_se` 0.0056 → 0.0249, matching the hand-computed figures exactly; the two single-series VCB runs are **unchanged** (5.50 / 0.96), because `_ic_uncertainty` had the `n_eff` formula right all along. One regression test pins the ratio at `√h` and that `evaluate_panel` pipes the horizon through. | `result_evaluator/metrics.py:479`; read by `index.py:152-153` |
| **UNI-1** | ⚠️ **A CROSS-SECTIONAL RUN'S UNIVERSE IS RECORDED AND THEN IGNORED.** `cs_rank_{h}day` is a rank WITHIN a chosen set of names, so the label means nothing without that set — and since 2026-08-18 the run folder does carry it (`metadata.json → input.universe`, 150 tickers for the top-150 run). **`final_features` never reads it.** It builds from the pools of `unified_schema_<ticker>`, which for the `ALL` schema is **781 names**, so a table built from a 150-name shortlist would re-rank over 781 and the model would be trained on a label the selection never scored. That is `RNK-1`'s sibling: RNK-1 is the wrong COLUMN, this is the wrong POPULATION, and both arrive through the same handoff. ⚠️ It has not yet produced a wrong number only because no cross-sectional `__final__` table exists on `ALL` — `unified_schema_all` holds 23 tables and none of them is a `__final__` or a `pool__shortlist__`. Found 2026-08-18. **Fix**: `final_features` reads `input.universe` from the run metadata, filters the build to it, and writes it into the table `COMMENT` so a dataset built later cannot silently widen it. ✅ **FIXED 2026-08-18 (P0-6).** `_read_outstanding` reads `input.universe`, it is a GROUP KEY so two populations cannot union (they collide on the table name and raise — pass `--scope`), `build_sql` emits `WHERE base.ticker IN (…)` with every name validated as an identifier first, and `comment()` records the first eight names plus a sha1 of the sorted set. Seven new tests in a package that had none. | `final_features/builder.py`; `feature_selection/report.py::build_metadata` (which already records it) |
| **RNK-1** | ⚠️ **A CROSS-SECTIONAL CHAIN SELECTS FEATURES FOR THE RANK AND THEN TRAINS THE MODEL ON THE RAW RETURN.** `final_features` deliberately does not store `cs_rank_{h}day` — its value depends on which names are in the panel and on `min_width`, properties of the RUN and not of the row (`final_features/CONTEXT.md` §5) — and stores `return_{h}day` instead, on the stated understanding that *"the reader re-ranks"*. **No reader re-ranks.** `train_test_creator.dataset` sets `stored_target = return_{h}day`, records `selected_for = cs_rank_{h}day`, and builds `y` from the STORED column (`dataset.py:521`, `:562`); a grep over `src/model/` and `src/result_evaluator/` for any within-date ranking returns **nothing**. Confirmed on a live artefact rather than by reading: `bank__rank_5day__final__d20_h5__basic…/metadata.json` reads `target.column = return_5day`, `selected_for = cs_rank_5day`, `derived = true`. ⚠️ **The consequence is measured, in §2b, on the same panel and the same folds**: swapping `cs_rank_5day` for raw `return_5day` drops the IC **4×** and pushes the hit rate below a coin. So the model is aimed at a label the selection never scored, and **§5d's *"the selection cleared its bar; the model did not clear its own"* now has a candidate explanation nobody had written down.** ⚠️ It has never produced a wrong NUMBER, which is why it survived: `result_evaluator.daily_ic` is a per-date Spearman, so SCORING stays cross-sectional even when training is not — the defect costs model quality, not evaluation validity. Found 2026-08-18 while asking whether the top-150 result could go straight to the model stage. **Fix**: re-rank at dataset build time for a `derived` target (the universe is the table's own ticker set on each date), and record that universe in the dataset metadata. ✅ **FIXED 2026-08-18 (P0-5).** `train_test_creator._label` reconstitutes the label by calling `cross_sectional.cross_sectional_rank` — one definition, not two, asserted equal at **atol = 0** — dropping and counting dates too thin to rank as `rows_unrankable`, and recording the recipe in `metadata.json → target.label_recipe`. `target.column` now means WHAT `y` IS; `target.stored_target` carries the provenance. Four new tests, 27 in the file. | `train_test_creator/dataset.py` (`resolve_target`, `_prepare`); `final_features/builder.py` §5 |
| **KGP-1** | ⚠️ **THE KAGGLE PAYLOAD'S `utils` STUB SHADOWED A PACKAGE THAT WAS NEVER SHIPPED, AND THE JOB HAD BEEN BROKEN FOR TWO DAYS.** `kgpu_bootstrap._install_stubs` fakes nine repo modules so `feature_selection.unified_reader` can import without psycopg2, and `utils` is on that list for **one constant**, `utils.constants.DATABASE_MAIN_V2`. The stub is a module with `__path__ = []`, so any real submodule under it becomes unreachable — and `feature_selection/report.py` gained `from utils import runtime` on **2026-08-15**, the day AFTER this integration's only green round trip. Every job would have died on the worker at `import feature_selection.report` with *"cannot import name 'runtime' from 'utils' (unknown location)"*, after the queue and the upload. Nothing detected it because nothing had rehearsed since; `python -m kgpu rehearse feature-selection` reproduced it in **3.6 s** on the first try of the panel work. ⚠️ **AND IT WAS NOT HYPOTHETICAL — CONFIRMED FROM KAGGLE'S OWN LOG, 2026-08-18.** The `mt-feature-selection` kernel sits in **ERROR**, and its last line before the nbconvert noise is verbatim *"---> 71 from utils import runtime … ImportError: cannot import name 'runtime' from 'utils' (unknown location)"*. A real run was therefore pushed after 2026-08-15, died on the worker, and **nobody read the log** — which is how a two-day outage stays invisible in a batch integration. **Fixed two ways, both needed**: `src/utils` is now in both jobs' `data.source_dirs` (34 .py travel, up from 26), and `_install_stubs` installs a stub **only when `importlib.util.find_spec` cannot find the real module** — a stub that shadows a shipped package fails at the import that needs it rather than at the one that is absent, which is why this looked like a `feature_selection` bug. ⚠️ Same shape as §7's four measured traps: **a green step is not evidence that the step did what it said** — and here there was not even a green step, only two days of nobody looking. | `kgpu rehearse feature-selection`, both mount layouts (2026-08-17) |
| **MTH-1** | ⚠️ **THE ENSEMBLE'S MEMBERSHIP CHANGED THE ANSWER AND WAS NOT IN THE GROUPING KEY.** The default ranker set narrowed from six to three on 2026-08-16 (`feature_selection/CONTEXT.md` §19), which changes which channels a run keeps — but `methods` was recorded in `metadata.json` only, never in `contract.SETUP_KEYS`, which is what `final_features.plan_from_reports` groups on. **So a pre- and a post-2026-08-16 run could be UNIONED INTO ONE TABLE with nothing saying so.** It was left open because adding a key to `SETUP_KEYS` makes every run archived without it ungroupable — `validate_shortlist` raises on all of them, `final_features` cannot plan, and every table below goes stale (the STL-1 domino). **Fixed 2026-08-16, and the archive did NOT have to be invalidated to do it.** `methods` joined `SETUP_KEYS`, and `contract.LEGACY_SETUP_DEFAULTS` gives a run that recorded no ensemble the reading `"unrecorded"` — so the 19 archived country runs still validate and still plan, while grouping APART from both the three-ranker default and a deliberate `methods=ALL_METHODS` reproduction. ⚠️ **`"unrecorded"` is deliberately NOT read as "the six"**, though those runs did use six: CLAUDE.md §5 rule 2 is that an absent measurement is recorded as absent, never inferred, and writing "the six" would also merge a legacy run with a genuine six-ranker reproduction. ⚠️ **Grouping is order-insensitive** (`contract.canonical_methods` sorts): the ensemble is a MEAN over its members, so member order cannot change the answer and must not split a table. ⚠️ **Consequence to expect on the next run:** a new three-ranker `vcb__basic` run and the 19 archived runs are now two groups that still want ONE table name, so `plan_from_reports` RAISES on the collision — which is the guard working. Pass `--scope basic`. **Measured:** archive alone plans 1 group / 19 runs / 885 features; add one three-ranker run and it separates and raises rather than silently unioning. | `feature_selection/tests/test_contract.py` (19 tests) |
| **WID-1** | ⚠️ **`gold.forex` NEEDED 3,130 COLUMNS AGAINST POSTGRESQL'S 1,600, so 90% of the forex on disk could not reach the database.** **Fixed 2026-08-14 by the split `gold.economy` already uses**: `gold.forex_<exchange>`, **48 panels / 3,129 series**, widest `forex_fx_idc` at **648 columns**. The builder checks the ceiling BEFORE writing anything and names the next lever if it is ever breached, builds one exchange at a time (the old version read all 13.7 M silver rows into one frame to pivot), and DROPS the un-suffixed `gold.forex` last, on success only — two answers to "what is gold forex" is the STL-1 shape. `unified/pool__forex` followed to `pool__forex_<exchange>` (48 pools per universe) and drops its own pre-split table the same way. ⚠️ `gold/forex` also left `WIDE_PANELS`: that generic builder asserts one row count, one column count and one date range, all true of a single table and false of a family. ⚠️ **Unlike economy, these panels do NOT share a calendar and it is not asserted** — economy reindexes every country onto one business-day range, but brokers quote what they quote (B2PRIME starts 2015, SAXO 2000), so equal row counts here would be a bug rather than a guarantee. | silver 13,662,058 rows / 3,129 series; gold 48 panels summing to 3,129, legacy table gone, round-trip 0 mismatches |
| **NUL-4** | ⚠️ **THE EMPIRICAL p-VALUE WAS ANTI-CONSERVATIVE BY EXACTLY ONE DRAW.** `NullResult.p_value` computed `max(k, 1) / (n + 1)` instead of the add-one estimator `(k + 1) / (n + 1)`, where `k` is the number of shuffled draws reaching the observed. Flooring the COUNT rather than adding one makes **k = 0 and k = 1 report the same p** — so "no shuffled draw beat the real data" and "one did" both printed the `1/(n+1)` floor, which at 20 draws is `0.0476` and reads as significant. Every `k >= 1` was understated: 1 -> 0.0476 (should be 0.0952), 2 -> 0.0952 (0.1429), 3 -> 0.1429 (0.1905). **Found by the `basic+economy_japan` run of 2026-08-10**, which reported `p = 0.0476` while its own `null_max` of **+0.0916** sat far above its `observed` of **+0.0509** — a p at the floor asserts no draw beat the observed, and the max said otherwise. **Four of the five archived nulls were affected; only japan's crosses 0.05** (0.0476 -> **0.0952**, so it does NOT clear). ⚠️ **The `z = +2.15` `vcb__basic` result behind EVD-1 is NOT affected** — its `null_max` (+0.0648) is below its observed (+0.0783), so `k` really is 0 and `p = 0.0476` stands. ⚠️ **The archived `metadata.json` files still carry the old p and cannot be recomputed from disk** — the raw draws are not stored, only their summary. Read `null_max` beside `p_value` on any pre-2026-08-10 run. | `feature_selection/tests/test_evaluation_null.py` (7 tests) |
| **ENC-3** | `result_evaluator.metrics.verdict` puts a `⚠️` in its **clearing** branch and nowhere else, and a Windows console is cp1252 — so **the only code path that could raise `UnicodeEncodeError` was the one reporting a positive result**, and it had never fired because no run had ever cleared a bar. The first that did (`baseline_ar`, `dir_auc_clears=True`) crashed the process *after* `append_run`: the run was recorded, the report was not. `engine._console_safe` reconfigures both streams with `errors="replace"`. ⚠️ Third instance of §5 rule 18 in two days (ENC-1 reading, ENC-2 writing, this one writing only on success) — the pattern is that the rare branch is the one that carries the glyph. | the clearing verdict prints |
| **ZRO-1** | `baseline.ZeroPredictor` emitted `0.0`, but every estimator works in the SCALED target space and `_write_predictions` inverts on the way out — so it predicted the **train mean return**, not zero, and was identical to `BASELINE_MEAN`. Caught only because its `RMSE` (0.03726) disagreed in the fourth decimal with the `RMSE_zero_baseline` column (0.037212) it should have equalled exactly. It now asks the target scaler which scaled value maps to a zero return. **The two numbers being identical is the standing self-check.** | `BASELINE_ZERO.test_RMSE == test_RMSE_zero_baseline == 0.037212` |
| **IDX-2** | `index.csv` had **two writers with two schemas**: `registry.append_run` wrote `model_type`/`val_ic`/`test_ic` after a run, `result_evaluator.rebuild_index` wrote `model`/`ic`/`dir_auc` (test split only) — so `--rebuild-index` silently *replaced the leaderboard's schema*, and a reader written against a trained row broke against a rebuilt one. IDX-1 fixed two metric ERAS inside the file; this fixes the two writers that could recreate them. One schema now lives in `result_evaluator/index.py` and both paths fill it through `index_row`; `leaderboard()` keeps its own shape as a read-only VIEW. | append and rebuild produce the same 43 columns |
| **CFG-1** | `pipeline._config_path` resolved `--config` against `model/lstm/configs/` alone, so the flag could not reach a second architecture — `--config cnn__…yaml` reported `no config at …/lstm/configs/cnn__…yaml`. Generalised to glob `model/*/configs/`, which immediately created the *second* half of the bug: the CNN config was first named `vcb__return_5day__final__d20_h5__basic.yaml`, identical to the LSTM's, and `sorted()` put `cnn` first — so the LSTM config became unreachable through that function while still resolving through `model.lstm.train.CONFIG_DIR`. **Two ways to name one file that disagree** is the STL-1 shape. Now: ambiguity **RAISES** and names every match; configs are prefixed with their model, as `run_name` already is; and `apply_model` dispatches on the config's directory rather than on `config["model"]["type"]`, which is a label a person edits. | all 4 configs resolve to the right package |
| **DUP-2** | `model/CONTEXT.md` §7 told a new model to write "a `train.py` copying `lstm/train.py`" — 346 lines of which **eight** were model-specific. Following it would have duplicated `_verify`, `_write_predictions`, the lineage block and `_registry_row` per architecture: the shape of **TGT-1**. Extracted to `model/common/engine.py`; `lstm/train.py` and `cnn/train.py` are ~30-line bindings and the LSTM's public names (`CONFIG_DIR`, `RUNS_DIR`, `load_config`, `train`) are unchanged so `pipeline/stages.py` and `RUN__lstm.ipynb` still import them. ⚠️ The behaviour that most needed protecting is `_write_predictions`: it inverse-transforms regression predictions to the RETURN scale, and it inserts the `ticker` column without which an N-ticker panel scores as one series (**PNL-1**). | LSTM dry-run unchanged; 38 tests pass; CNN and LSTM rows in `index.csv` produced by identical code |
| **RUN-1** | `pipeline.status_model` matched run folders with `name.startswith(run_name + "__")`, but a folder is `<run_name>__<YYYYmmdd-HHMMSS>` — so the prefix also matched `<run_name>__<anything>__<stamp>`. Latent until two configs shared a prefix; the moment `lstm__vcb__return_5day__final__d20_h5__basic` existed, the WIDE config reported **2 run(s)** and named the basic run as its own latest. Now anchored on the timestamp, so the two configs' run sets are disjoint. | the wide chain reports `1 run(s) … latest …__d20_h5__20260809-130032` |
| **ENC-2** | `python -m train_test_creator` died with `UnicodeEncodeError` before printing its plan: `main` prints `⚠️` per dropped channel, a Windows console is cp1252, and U+26A0 has no code point there. Only the CLI could hit it (a notebook's stdout is UTF-8) and only when a channel WAS dropped, so the wide table's 26 drops never surfaced it through the notebook path. `main` now reconfigures both streams with `errors="replace"`, degrading the glyph instead of discarding a finished build. ⚠️ Same rule as ENC-1, opposite direction: that one was READING utf-8, this one is WRITING it. | `python -m train_test_creator --table …__basic` prints its plan and saves |
| **ROO-1** | `pipeline.apply_selection` called `outstanding.main([])`, passing `[]` as the report ROOT — `os.listdir([])` would raise. Never fired because the selection stage is always `ready`, so `--apply` skipped it every time. Fixed while threading `--root` through. | `apply_selection(root)` |
| **EMP-1** | `pipeline.status_data` compared `raw_date > table_date.isoformat()` as strings and used `"none"` when the table was empty — `"2026-08-07" > "none"` is False, so a table with no rows reported itself **current**. Now `table_date is None` is stale by construction. | `behind = table_date is None or raw_date > stamp` |
| **PIP-1** | Feature selection is manual by design, so `--apply` cannot cold-rebuild — but **`Stage.manual` was set and never read**. The plan decided "MANUAL" from `stage.apply is None`, and the selection stage *has* an apply (it refreshes shortlists), so the flag was dead and the plan printed a bare `ran`. The design is unchanged; it is now **stated**: a `manual` column in the plan, `ran (refresh only)` when a manual stage applies, and `MANUAL — cannot be produced here` when one is not ready. | `python -m pipeline` → `manual` column reads `True` on `selection` only |
| **STL-1** | The VCB table had drifted 26 columns from its own shortlists and nothing noticed, because "the table exists" was the only check. Now every table's `COMMENT` carries a **fingerprint** — a sha256 over its sorted `(source_table, channel)` set — and `status_final_features` compares it against what the current reports would produce. `max_features` was dropped from `SETUP_KEYS` (it said `12` while runs kept 10–236) and the real cut parameters are stamped into `outstanding.csv`. Both tables dropped and rebuilt: **VCB 203 → 750 channels**, bank 10 → 14. | `status_final_features` → `current — fingerprint 505fbe21a1f0 matches` |
| **CMP-1** | Classifier core metrics were measured against the 0/1 label. The old fallback *could not* have worked — a classification dataset's `y_test` **is** the label, so `load_dataset` returned the thing it was called to replace, silently. Now read from **`pool__targets`** on `(date, ticker)`; when no schema is recorded, `ic` and `long_short` are **NaN** rather than a number in the wrong units, while `dir_auc`/`hit_rate` survive (they need only the up/down label). | all 18 classification rows: `ic` NaN, `dir_auc` present, `return_source = unavailable (no schema recorded)` |
| **BNK-1** | The bank table could not enter the pipeline: the reader trusted the table NAME for the target. Whole chain now runs — 26,964 / 12,524 / 13,028 windows, **no skill on either split**. | `test_a_rank_table_reads_the_column_it_actually_stores` |
| **TGT-1** | The first BNK-1 fix rebuilt `return_{h}day` from the horizon, duplicating `final_features._stored_target` where it could drift. Replaced by two authoritative records: `outstanding.csv` says what is a **channel**, `pool__targets` says what is a **label**. No horizon string is constructed anywhere. | `test_no_horizon_string_is_ever_constructed` |
| **PNL-1** | An N-ticker panel scored as one series was wrong three ways, each flattering the model: `n_eff = n/h` counted 20 banks on a date as 20 observations (2,606 vs 130.6); a pooled Spearman mixed "which bank beats which" with "is today a good day"; the row-block null tore each date's cross-section apart. `evaluate_panel` fixes all three; grain is detected from the `ticker` column, never a config flag. | 5 tests incl. `test_panel_n_eff_counts_dates_not_rows` |
| **NUL-2** | `block_shuffle` pads with NaN; permuted forward they landed in the retained slice, so **10 of 200 draws survived** while the p-value was still divided by 201 — understating every p-value ~20×. Bar `+0.046 → +0.113`, `p = 0.025 → 0.522` on the VCB run. | `test_every_draw_is_usable_despite_the_shuffle_padding_with_nan` |
| **EFF-1** | `n_eff` priced in label overlap only. `n_eff_windowed = n/(d+h-1)` — the purge-gap count, the point at which two windowed samples truly share nothing — is now reported beside it (127.0 vs **26.5** on 635 test samples). Both are kept: diverging from `feature_selection.effective_sample` silently would be worse than being optimistic openly. | reported per split |
| **NAM-1** | `dir_accuracy` named two quantities — the legacy sign hit rate at 0 and the core's hit rate at the score's median. The core metric is now **`hit_rate`**; `dir_accuracy` unambiguously means the legacy one. | `CORE_METRICS = ("ic", "dir_auc", "hit_rate", "long_short")` |
| **IDX-1** | `index.csv` had accumulated two metric eras and a naive `dropna()` silently picked one. `python -m result_evaluator --rebuild-index` regenerates it in one pass from every run's predictions — one column set, one definition. | 29 rows × 52 columns, single era |
| **DUP-1** | Three superseded run folders removed; they referenced datasets that no longer exist after the STL-1 rebuild. | 5 `d20_h5` folders → 2 |
| **PKG-1** | `src/evaluator/result_evaluator.py` deleted — unused, and its name collided with the package. `data_evaluator.ipynb` kept; it is a different tool. | `src/evaluator/` holds one notebook |
| **OLD-1** | `unified_schema_creator.ipynb` (1.2 MB) deleted — superseded by `orchestration/assets/unified.py`, imported by nothing, recoverable from git. | gone from `src/train_test_creator/` |
| **OLD-2** | 27 legacy configs moved to `configs/_legacy/` with a README stating what changed and warning that a legacy run lands in `index.csv` with the core columns blank until `--rescore`. | `configs/` holds the 2 current files |
| **TCK-1** | `tickers_*.npy` was a dtype-`object` array, so `np.save` pickled it and every reader uses `allow_pickle=False` — the file existed and was unreadable, which is exactly how a panel gets silently scored as one series. | `test_the_ticker_array_is_unicode_not_object` |
| **HDR-1** | `csv.DictWriter` writes fields in the code's order; appending under a differently-ordered header silently misaligns every new row. `append_run` now migrates the header. | migration verified on the live `index.csv` |
| **ENC-1** | `model/common/data.py` opened `metadata.json` with a bare `open()` — cp1252 on Windows — and the provenance `COMMENT` carries a `⚠️`. | notebook executes end to end |
| **GIT-1** | Dropping the `reports/feature_selection/*/` rule was not enough, and the 384 already-indexed files hid it: a **new** run's `.csv` was still caught by the blanket `*.csv`. Negations added after it. | exit-code test: new run folder tracked, controls still ignored |

## Priority

✅ **`WID-1` WAS CLEARED THE DAY IT WAS OPENED (2026-08-14)** and it went exactly as
priced: no scraping, one code change, and the chain loaded **3,129 series instead of
357**. Clearing it exposed **`SHP-1`** underneath — the blocked ingest had been hiding a
silent one, and 71% of the folder had been discarded on every previous run.

⚠️ **`SHP-1`'s UNCHECKED HALF IS THE OPEN QUESTION.** `bonds`, `funds`, `economy` and
`indices` carry the same `value`-only filter and the same two-shape scraper, and none has
been counted. The forex number was 4,402 files of 6,189. **`FLT-1` is the wider prize and
the harder one**: 37 of 47 brokers' books are unreachable until a Selenium filter
injection is diagnosed, and no amount of re-running reaches them.

⚠️ **THE THREE OPENED 2026-08-16 (`PNL-2`, `CSP-1`, `MEM-1`) ALL BLOCK THE SAME RUN**,
and they were found together when a cross-sectional layer-1 sweep was requested on
`unified_schema_all`. Read them as one wall rather than three items: §2b's ladder says
width is the only thing that has ever cleared a null, and **every path to a wide run is
closed** — the grain is chosen by a name (`PNL-2`), the one correct grain can read one
pool (`CSP-1`), and the panel that grain needs does not fit in memory (`MEM-1`).

Their fixes are cheap, medium and structural in that order:

- **`PNL-2`** is the cheapest fix in the register: derive `cross` from the panel's own
  `ticker` count, the way `PNL-1` already made the SCORER do it, and raise when a
  multi-ticker panel meets a non-`cs_` target instead of silently pooling it. Half a day
  including a test.
- **`CSP-1`** is a real but bounded change: give `read_universe_panel` the `UnifiedSchemaReader.join()`
  the single-ticker path already uses, so `--pools` means the same thing at both grains.
  ⚠️ It makes `MEM-1` worse by exactly the width of whatever is joined — `pool__ta` at
  922 channels would be ~10× the design.
- **`MEM-1`** is the structural one and the only one worth doing carefully. `float32`
  bought a factor of two and is **unverified**; the real fix is to stop materialising the
  whole design — window per fold, or per ticker-chunk, and never hold the blocks and the
  concat at once. Until then the honest options are a narrower universe
  (`read_universe_panel` already takes a `tickers` list and filters in SQL, so the top-300
  by turnover is a CLI flag away, ~1.3 M rows) or a machine with more than 15.6 GB.

⚠️ **The cheap first step is the measurement nobody has**: `read_universe_panel(tickers=<top 300>)`
with `cs_rank_5day` fits today, needs none of the three fixed, and would put a real number
against §2b's `ALL` row — which currently reads **"never ran — ⚠️ unverified"**.

The four older substantive ones are all upstream of the code:
**EVD-1** and **NUL-1** are about what a null can prove, **DRF-1** and **COV-1** are
defects in the *selection*, not in the stages that consume it.

⚠️ **What is left open is what is EXPENSIVE or STRUCTURAL, not what is unknown.**
The 2026-08-09 pass took every cheap thing: EVD-1's one affordable null was measured
(it failed, `z = +1.46`), COV-1's true extent was measured and stamped into the
deliverable (248 of 952 rows), and PIP-1 turned out to be dead code and closed. The
remainder is priced: EVD-1's other 18 nulls are 1,000+ CPU-hours, NUL-1 cannot be
fixed by an evaluator that does not re-run the selection, DRF-1 needs differenced
channels upstream, and RPR-1 is a deliberate trade-off.

⚠️ Note what none of the 18 fixes changed: **every run in the archive still sits
inside its own null.** The STL-1 rebuild made the VCB result worse, not better —
`test ic −0.011 → −0.072`, `r2 −0.08 → −0.90` — which is the expected consequence of
handing an LSTM 724 channels instead of 202 on 2,918 training windows.
