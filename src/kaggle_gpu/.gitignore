# Secrets — never commit
.env

# Generated by kgpu: the staged notebook, the staged payload, the raw download,
# and the local rehearsal tree. All four are rebuilt by a command.
.build/
.payload/
.rehearsal/
results/
local_runs/
*.log

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# ⚠️ A SECOND VENV IN src/. `test_env/` was this package's own 137 MB
# environment, from when it was a standalone demo. It is no longer used —
# `mt_env` is, because the export step imports the repo. Delete it.
test_env/
.venv/
