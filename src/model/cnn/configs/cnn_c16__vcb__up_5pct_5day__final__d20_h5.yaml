# Written by `python -m event_chain` for up_5pct_5day (1 when close_adjust[t+5] >= (1 + 5%) x close_adjust[t], else 0; NULL for the last 5 sessions).
# The grid is event_chain/config.py MODELS.
run_name: cnn_c16__vcb__up_5pct_5day__final__d20_h5
dataset: vcb__up_5pct_5day__final__d20_h5__tr70_val15_test15__std
dataset_hash: 15720a134545481e
task: classification
lookback: 20
n_features: 183
model:
  type: CNN
  channels: 16
  kernel_size: 3
  num_layers: 2
  dropout: 0.2
null_draws: 200
seed: 42
device: auto
train:
  batch_size: 64
  lr: 0.001
  weight_decay: 0.0001
  max_epochs: 100
  patience: 15
  grad_clip: 1.0
  lr_factor: 0.5
  lr_patience: 5
  log_every: 10
